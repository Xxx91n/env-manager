已完成开工勘察：票 14 的 Blocked by 为“无”（主线已收口合入 origin/main，可直接开工）；必读清单标题为 Handoff 14 — CLI 输出快照化（Verify）、14 — CLI 输出快照化（Verify：help / 错误文案 / canary 输出）、Spec — Env Manager C# 引擎架构恢复（含 Phase 3 — 测试心智模型升级）、架构恢复工作流（architecture-recovery）、Hard Boundaries (Red Lines)，并核对了调研依据“下一波架构/测试心智模型调研”。当前 CodeGraph 索引已是 up to date。尚未修改文件。

实施计划：
1. **隔离票 14 与并行工作**
   - 先用 GitButler 只读确认当前 dirty 区；现有 `.config/dotnet-tools.json`、ADR 0014、`stryker-config.json`、`EnvManager.Engine.Tests.csproj` 等改动不属于票 14，绝不吸收、回滚或改写。
   - 在不动其他栈的前提下创建独立 `arch/14-cli-output-snapshot-testing` GitButler 分支；所有版本控制严格遵循 WORKFLOW §4.2，并在收尾只提交本票文件。

2. **A：引入 Verify.Xunit（C#）**
   - 在 `tests/EnvManager.Engine.Tests/EnvManager.Engine.Tests.csproj` 增加固定版本的 `Verify.Xunit`，仅通过 restore/build 验证兼容性，不升级无关包；保留现有 xUnit/Test SDK/CsCheck 配置。
   - 若当前 csproj 的并行票修改与本票同文件造成归属冲突，按 GitButler 文件/区块精确分离，只保留 Verify 引用及票14测试所需内容。

3. **B：锁定 CLI 人读输出**
   - 在 `src/Program.cs` 将 `ShowHelp()` 的固定文本提炼为可测试的 `internal` `BuildHelpText()`（或等价最小 seam），`ShowHelp()` 继续保持原有 stdout 与返回码行为；版本号作为动态字段交给 scrubber，不能把真实 help 文案静默删掉。
   - 新增专用 `CliOutputSnapshotTests.cs`：串行捕获并恢复 `Console.Out`/`Console.Error`，覆盖 help、稳定命令 stdout、错误文案，以及 canary 相关的 `<encrypted>` 与 `<revealed>` 展示契约。涉及注册表/进程全局初始化的路径不直接调用完整 `Main`；优先使用现有 internal 纯函数、命令 core、`InMemoryScope`/记录 seam。对确需 CLI 行为的错误路径使用最小子进程/稳定输入，避免真实注册表写入。
   - 快照按 output category 组织，使用 Verify 的 `.verified.txt` 文件；快照中保留所有 user-facing 文案和 canary 占位符，便于 review 发现文本漂移。

4. **C：Verify scrubber 稳定化**
   - 在测试侧集中注册 Verify scrubber，只规范化已确认易变字段：进程 PID、时间戳/UTC 时间格式、临时目录或动态程序集版本（必要时）；替换为明确占位符（如 `<pid>`、`<timestamp>`），不使用宽泛正则吞掉文案。
   - 增加回归断言/快照样例证明：同一输入二次运行得到稳定结果；PID/时间戳被替换；`<encrypted>`、`<revealed>`、`<redacted>` 仍原样保留；普通 user-facing 文案未被 scrub。

5. **D：i18n 每 locale 全键渲染快照**
   - 强化 `frontend/src/lib/translations.test.ts`：递归 flatten 10 个 JSON 的叶子路径，校验缺失/extra、object/string 类型冲突、空值和 ICU placeholder 集合；不再只比较 426 个顶层 key。
   - 增加稳定排序的全键渲染快照测试：每个 locale 一份 Vitest snapshot（10 locale），快照记录完整叶子 key 与渲染文本。由于全局 `svelte-i18n` mock 只加载 en/zh 且不执行 ICU，测试将直接使用全部 10 个 JSON，并用固定插值夹具渲染 `{name}`/`{count}`/`{version}` 等占位符；保留 ICU 单引号语义，绝不把 `{placeholder}` 包进单引号。缺失或格式不支持时让测试失败而不是静默回退英文。
   - 对复杂 ICU 条目采用可审阅的确定性 renderer/参数表，并单独断言 placeholder 集；这样快照承担“人读文案漂移”而不是替代结构完整性测试。

6. **E：接入现有 CI 并验证**
   - 不新增独立快照更新 CI 模式：现有 verify job 的 `dotnet test ... -c Release` 与 frontend `npx vitest run` 会自动发现新测试；CI 只验证已提交快照，快照更新必须在本地显式生成并审阅。
   - 本地依次运行并记录原始结果：`dotnet restore`（依赖变更后）、`dotnet build -c Release`、`dotnet test tests/EnvManager.Engine.Tests/EnvManager.Engine.Tests.csproj -c Release --nologo`、`cd frontend && npx vitest run`，并运行 `git diff --check`、行尾/BOM检查；测试第二次运行确认快照无漂移。代码变更后执行 `codegraph sync .`。
   - 按 AGENTS.md 要求运行 `node scripts/build.mjs --arch x64` 并检查 release 产物；如环境/并行栈导致不可运行，报告中记录真实命令、退出码和未覆盖项，不用代理信号冒充通过。

7. **交付报告与收口**
   - 生成 `.scratch/architecture-recovery/reports/14-cli-output-snapshot-testing.md`（使用 Node.js/ctx 写入），逐条对应 issue 的四项验收：Verify 引入与四类 CLI 快照、PID/时间 scrubber、人读文案显式 diff、10 locale 全键快照、dotnet/Vitest/CI 门禁；每条附本次收尾的当场命令输出和快照文件清单。
   - 收尾用 `but diff`/`but status -fv` 做完成审计，确保只含票14；按 WORKFLOW §4.2 用 conventional commit 提交到票14分支。遵守该工作流“不 push、不建 PR（除非用户明确要求）”，因此不执行 push；同时不修改 Wave README 或生成 review 文件（那是大脑会话验收职责）。

验收停止条件：Verify 包已固定且 restore 成功；C# help/stdout/error/canary 快照、scrubber 回归和 10 locale 全键快照均存在并能重复通过；`dotnet test` 与 `vitest` 全绿；构建/CodeGraph/行尾检查有真实输出；报告逐项落盘；GitButler 提交只包含票14变更，其他并行改动仍保持原样。