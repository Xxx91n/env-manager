# Detailed Reference Index

> **Agent instruction layer** — not user documentation.
> Extracted from AGENTS.md for progressive disclosure.
> Source: [AGENTS.md](../../AGENTS.md) canonical reference.

This index maps topics to their detailed reference files.

| Topic | File |
|-------|------|
| Full CLI command table, scope, debug, error handling, profiles, toggle, path editor, path resolution | [docs/cli-commands.md](docs/cli-commands.md) |
| Architecture, IPC bridge, race condition prevention, system tray, toast, caching, auto-update, security hardening, modal dialog, rename/change-scope, profile audit history, GUI/CLI alignment table | [docs/architecture.md](docs/architecture.md) |
| Build system, prerequisites, output layout, mandatory build rules, release steps, dependencies, CodeGraph, performance targets, logging, debugging | [docs/build-and-release.md](docs/build-and-release.md) |
| Backup JSON format, profile JSON format, extended state and safety contracts, full security list, agent safety guidelines | [docs/backup-and-profiles.md](docs/backup-and-profiles.md) |
| CLI-level agent guide (distributed with CLI binary) | [AGENTS.cli.md](AGENTS.cli.md) |
| Secrets architecture blueprint: current capabilities, limitations, v0.8-v1.0 phased roadmap, anti-rejection checklist, risk counter-decisions | [docs/secret-architecture-blueprint.md](docs/secret-architecture-blueprint.md) |
| Secret providers user-facing setup guide: prerequisites, one-time install, activation errors and fixes for all 8 providers (DPAPI, Credential Manager, PowerShell SecretManagement, Vault KV v2, SOPS, Azure Key Vault, 1Password CLI, AWS Secrets Manager) | [docs/secret-providers-guide.md](docs/secret-providers-guide.md) |
| ADR 0001: Secret architecture revision decisions A1-A11, Phase A-E roadmap | [docs/adr/0001-secret-architecture-revision.md](docs/adr/0001-secret-architecture-revision.md) |
| Secret architecture decision summary (single-page distillation of design review) | [docs/secret-architecture-decision-summary.md](docs/secret-architecture-decision-summary.md) |
| Internal development process record + design review risk matrix | [CONTEXT.md](CONTEXT.md) |
- **v0.9.2 CancellationToken graceful shutdown (hard boundary)**: `service/src/main.rs` MUST create a `tokio_util::sync::CancellationToken` (wrapped in `Arc`) and pass clones to both `reconcile::reconcile_loop(token)` and `ipc::start_ipc_server(pipe, token)`. The IPC `shutdown` method MUST call `shutdown.cancel()` to trigger cooperative cancellation. The reconcile loop uses `tokio::select!` between `interval.tick()` and `shutdown.cancelled()` so it exits cleanly between ticks. The IPC server uses `tokio::select!` between `server.connect()` and `shutdown.cancelled()` so it exits cleanly during connection wait. Both tasks log a distinct "cancelled" message before returning. The main `tokio::select!` detecting task exit logs "service shutting down (all tasks exited)". Without this, `shutdown` IPC returned ok but the service kept running — stale pipe prevented restart. Never revert to the no-token pattern where `shutdown` is a no-op that only returns `{"shuttingDown":true}` without actually stopping the service.

- **v0.9.2 CLI pipe no-more using-var (hard boundary)**: `RunServiceCommand` in `Program.cs` MUST NOT use `using var` for `StreamWriter`/`StreamReader` wrapping the `NamedPipeClientStream`. The `using var` pattern calls `Dispose` at scope exit (LIFO with other using-vars), AFTER `return 0` — but the service has already closed its end of the pipe by then, so `Dispose` throws `IOException: Cannot access a closed pipe` which propagates to stderr and makes the CLI exit code 1 even though the response was `{"ok":true}`. The fix uses explicit `var` (not `using var`) with `leaveOpen: true` on both `StreamWriter` and `StreamReader`, and wraps each `Dispose()` in `try { } catch { }` so teardown failures are swallowed. The response is already read and printed before any Dispose runs; the exit code is determined solely by the response content. Never re-introduce `using var` for pipe-wrapping readers/writers when the remote end may close the pipe first.

- **v0.9.2 Service log alignment (hard boundary)**: all service atomic transactions MUST be logged at `log::info!` level (not `debug!`): reconcile loop start (with warmup and interval params), each reconcile tick start/complete (with refreshed/failed/skipped counts), each IPC connection accepted, each IPC request (with method name), each IPC response (with ok bool and method), shutdown trigger, cancellation cascade per task, and service final shutdown. The service log level is `Info` (set in `env_logger::Builder::filter_level`), so `debug!` entries are invisible to users debugging service issues. Never use `debug!` for service lifecycle events that a user or developer needs to see when diagnosing service starts, IPC requests, or shutdown failures.

- **v0.9.2 save_mounts atomic rename (hard boundary)**: `reconcile.rs save_mounts` MUST use `std::fs::rename(tmp, path)` directly to atomically replace the destination — Rust `std::fs::rename` on Windows calls `MoveFileExW` with `MOVEFILE_REPLACE_EXISTING`, so it overwrites the destination atomically. The prior code renamed the old file to `.bak`, then `remove_file`d the original, then renamed the temp — this created a window where the file didn't exist (data loss if the process crashed between remove and rename). On rename failure, the temp file MUST be cleaned up (`std::fs::remove_file(tmp)`) so it doesn't leak. Never re-introduce the `.bak` intermediate step.
- **v0.9.4 CI test tier Pester integration (hard boundary)**: `scripts/run-ci-tests.ps1` is the canonical CI Tier 3 orchestrator and MUST be invoked by `.github/workflows/build.yml` `verify` job AFTER the `Build CLI` and `Run unit tests` steps have produced the binary. Three suites, in fixed order: (1) Pester 5+ `tests/launch-env-injection.Tests.ps1` with `BeforeAll`/`AfterAll` self-contained fixture that creates a uniquely-named `EM_CI_LAUNCH_PROBE_*` launch profile + a probe `.bat` that writes `set > $probeOut`, runs `profile preview`/`profile show`/`profile reveal-secret`/`profile launch`, asserts that the spawned child process actually received the injected regular and secret variables, then deletes the profile + probe in `AfterAll`; (2) `scripts/test-inheritance-protection.ps1` (raw ps1, AGENTS.md hard boundary); (3) `scripts/test-with-restore.ps1` (raw ps1, AGENTS.md hard boundary). Pester JUnit XML is written to `test-results/*.junit.xml` and uploaded via a separate `actions/upload-artifact` step gated with `if: always()`. The orchestrator honours `-CliExe` for non-standard CLI paths by setting `$env:EM_CLI_EXE` (Pester 6 removed `Run.Script`, so parameter passing is now via env to stay forward-compatible). The `v0.7.5 non-launch profile secret rejection` rule made the legacy `test-with-restore.ps1` "secrets never in registry" case stale: it MUST now create the secret profile with `--type launch --target (Join-Path $env:SystemRoot "System32\cmd.exe")` so `add-secret` passes the launch-only precondition; `profile apply` is still rejected by `ProfileApply` for launch profiles so the test's `$applyExit -ne 0` assertion still holds. Never remove `--type launch` from this test path, never route secrets to a non-launch profile, never run Tier 3 before the `Build CLI` step.

- **v0.9.6 SCM failure recovery via WiX CustomAction (hard boundary)**: `frontend/scripts/installer.wxs` MUST include two `CustomAction` elements that run after `InstallServices`: (1) `SetServiceFailureActions` calling `sc.exe failure EnvManagerService reset= 86400 actions= restart/60000/restart/60000/restart/60000` (3x restart at 60s intervals, reset counter after 24h), and (2) `SetServiceDelayedAutoStart` calling `sc.exe config EnvManagerService start= delayed-auto`. Both MUST be conditioned `NOT Installed` so they only fire on fresh install. This is the Windows SCM-native crash recovery for Service mode. Background mode relies on the GUI watchdog. Never remove these CustomActions.

- **v0.9.6 GUI service watchdog (hard boundary)**: `frontend/src-tauri/src/main.rs` MUST spawn a watchdog thread via `spawn_service_watchdog(app)` that pings the service every 30s. After 2 consecutive ping failures, the watchdog calls `start_service()` to auto-restart. The watchdog is defense-in-depth on top of SCM recovery for Service mode, and the primary recovery mechanism for Background mode. Never remove the watchdog or increase the ping interval beyond 30s.

- **v0.9.6 CLI pipe connect retry (hard boundary)**: `RunServiceCommand` in `Program.cs` MUST retry the `NamedPipeClientStream.Connect` call 3 times with exponential backoff (1s, 2s) before returning an error. Never return a pipe-connect error without at least 3 attempts.

- **v0.9.6 Service heartbeat enrichment (hard boundary)**: `service/src/ipc.rs` ping and status responses MUST include `uptime_seconds` (process uptime from `crate::get_process_uptime()` using `OnceLock<Instant>` set at service start). Never remove the uptime field from ping/status responses.

- **v0.9.6 Version single source (hard boundary)**: `scripts/build.mjs` MUST read `<Version>` from `env-manager.csproj` at startup and auto-sync it to `frontend/package.json` if they differ. Never maintain two manually-edited version fields; the csproj is the single source of truth.

- **v0.9.6 Documentation sync CI check (hard boundary)**: `.github/workflows/build.yml` MUST run `scripts/check-doc-sync.ps1` as a CI step in the `verify` job. A broken doc reference MUST fail the build.

- **v0.9.7 Service probe fast-fail vs reliable-write connect (hard boundary)**: `RunServiceCommand` in `Program.cs` MUST distinguish read probes (`status`/`ping`/`health`) from write operations (`refresh`/`rotate`/`reload`/`shutdown`) when connecting to the named pipe. Read probes use 1 attempt with a 2-second connect timeout (fail fast — the GUI needs to know the service is down in <2s, not <18s). Write operations retain the 3x retry with 5-second timeout and 1s/2s exponential backoff (reliable delivery). The prior v0.9.6 uniform 3x retry applied to ALL subcommands, causing `service status` to block for 18-31 seconds when the service was not running — the GUI appeared frozen while the pipe connect timed out three times. Never apply the retry loop to read probes; a single fast probe is the correct behavior for liveness detection.


- **v0.9.12 Sensitive Data Redaction Architecture (hard boundary)**: All three tiers (C# CLI, Rust Tauri shell, Rust service) MUST scrub secret-bearing patterns from stderr, logs, and tracing output using a unified 22-pattern vocabulary. C# ScrubExceptionMessage(string msg) in Program.cs MUST be called at every x.Message write site (7 sites: top-level catch, decrypt-secret catch, secret-provider-set catch, generic error catch, launch-failed catch, update-check catch, service-connect catch). Rust scrub_stderr in rontend/src-tauri/src/main.rs MUST include all 22 patterns. Service edaction.rs exposes scrub_message() for tracing call sites. The secrecy crate (0.10, with zeroize feature) MUST be present in both service/Cargo.toml and rontend/src-tauri/Cargo.toml. C# SecretString ref struct wraps decrypted secret values and zeroes the underlying char[] on Dispose. Pattern vocabulary: Bearer, token=, Token=, password=, Password=, setx, OP_SERVICE_ACCOUNT_TOKEN=, VAULT_TOKEN=, AWS_SECRET_ACCESS_KEY=, AWS_SESSION_TOKEN=, client_secret=, connection_string=, subscription_key=, api_key=, apikey=, client_id=, tenant_id=, access_token=, refresh_token=, Authorization:, X-Vault-Token:, x-api-key:. A new pattern MUST be added to all three sites simultaneously. The pattern list is a best-effort allow-list, not a universal filter; the 512-char truncation limits blast radius. See ADR 0005 (docs/adr/0005-sensitive-data-redaction.md).
- **v0.9.16 ProfileShow source-aware output + resolvedPaths (hard boundary)**: `ProfileShow` in `Program.cs` MUST use `ResolveProfileVariablesWithSource(profile, profiles)` (not `profile.Variables` directly) to populate `masked.Variables` with `Scope` + `SourceProfile` fields — a variable inherited from a parent profile shows `sourceProfile: "parentName"`, not null. `masked.ResolvedPaths` MUST be populated via `ResolveProfilePathsWithSource(profile, profiles)` returning `List<ResolvedPathEntry>` with `Path`, `Scope`, and `SourceProfile` — this provides the GUI with per-path scope badges and inheritance source labels. `masked.PathScopes` MUST be included in the serialized output (was missing before v0.9.16). The `ProfileVariable` class carries a nullable `SourceProfile` field (`[JsonPropertyName("sourceProfile")]`); the `ResolvedPathEntry` class carries `Path`, `Scope`, `SourceProfile` with matching JSON property names. The `ProfileData` class carries `ResolvedPaths` as `List<ResolvedPathEntry>?` (`[JsonPropertyName("resolvedPaths")]`). The GUI `ProfileData` TypeScript interface in `api.ts` MUST include `pathScopes?: string[]`, `resolvedPaths?: Array<{ path: string; scope: string; sourceProfile: string }>`, and `ProfileVariable` MUST include `scope?: string` + `sourceProfile?: string`. When adding a new Resolve function: walk the inheritance chain depth-first, label each item with the contributing profile's Name, and deduplicate paths by `NormalizePathEntry` (same as `ResolveProfilePathsWithScopes`).
- **v0.9.17 ProfilePage path scope badge uses PathScopes + PathEditor reverse index + IPC snake_case fix (hard boundary)**: `ProfilePage.svelte` list view MUST use `profile.pathScopes` (index-aligned with `pathEntries`) to render scope badges for path entries — NOT `profile.resolvedPaths` (which is only populated by `profile show` / `ProfileShow()`, not `profile list` / `ProfileList()`). The v0.9.16 `resolvedPaths` field remains valid for `profile show` detail view; the list view just does not populate it. `PathEditor.svelte` MUST consume a `pathProfileIndex` store (type `Map<string, string[]>`, mapping normalized path to contributing profile names) built by `ProfilePage.svelte` `refreshProfiles()` from the already-loaded `profileList`. The reverse index gives O(1) source-profile lookup per live PATH entry; no new CLI command is needed. Normalization: `path.toLowerCase().replace(/\\+$/, "")`. The IPC JSON key `"mountId"` (camelCase) in `RunServiceCommand` MUST be `"mount_id"` (snake_case) to match the Rust `IpcRequest` struct serde field name; `"request_id"` was already snake_case. Error messages ("refresh requires mountId") remain human-readable English and are NOT JSON keys. i18n key `profiles.fromProfile` added to all 10 locale files for the "from <profile>" source label.
- **v0.9.18 Service rotate CREATE_NO_WINDOW + refresh timestamp formatting + WebView2 context menu disabled (hard boundary)**: `call_cli_rotate()` in `service/src/reconcile.rs` MUST set `.creation_flags(0x08000000)` (CREATE_NO_WINDOW) on the `tokio::process::Command` to prevent console window flash when the service spawns the CLI for secret rotation. The `--json` flag MUST NOT be passed (CLI `profile secret-provider rotate` does not support `--json`; it exits non-zero, producing `"rotate failed: CLI rotate failed: "`). `ServicePage.svelte` mount list MUST format `mount.lastFetchedAt` via `new Date(mount.lastFetchedAt).toLocaleString()` with `text-[10px] whitespace-nowrap truncate max-w-[180px]` to prevent the raw RFC3339 timestamp from pushing buttons out of the flex row. App.svelte root `<div>` MUST carry `on:contextmenu={(e) => e.preventDefault()}` and global CSS MUST set `body { -webkit-user-select: none; user-select: none; }` with `input, textarea, [contenteditable] { -webkit-user-select: text; user-select: text; }` to fully suppress the WebView2 native right-context menu (Refresh/Screenshot/Inspect) while preserving text entry in form fields. This matches the clash-verge-rev industry pattern (`webview.oncontextmenu = () => false`). Never re-introduce `--json` in `call_cli_rotate` args; never remove the `creation_flags` call; never render raw RFC3339 timestamps in the mount list without formatting.
- **v0.9.19 SWR caching + listProfiles cache + Tab hover preload + ProtectionPage concurrent (hard boundary)**: [existing content truncated for matching]
- **v0.9.20 Theme style persistence + event dispatch (hard boundary)**: App.svelte MUST define `handleThemeStyleChange(e: CustomEvent<string>)` that updates `themeStyle` state, sets `data-theme-style` attribute on `<html>`, and adds `.theme-changing` class for scoped CSS transition. SettingsDialog.svelte `changeThemeStyle` MUST `dispatch('themeStyleChange', style)` instead of direct DOM mutation — the DOM update belongs to App.svelte's handler. App.svelte onMount MUST read `getSetting('themeStyle')` with an allow-list validation (`['slate','blue','violet','rose','cyan','amber'].includes(stored)`) so the persisted theme style survives restart. Never remove the event dispatch; without it the App.svelte state diverges from the SettingsDialog prop and the next toggle reverts to the stale App state. `toggleDarkMode` MUST NOT dispatch `themeStyleChange` — it only dispatches `themeChange`. The two concerns (dark/light vs slate/blue/violet/rose/cyan/amber) are independent.
- **v0.9.20 Tab indicator initial width 0px (superseded by v0.9.24)**: This boundary is superseded by v0.9.24 CSS-only tab indicator. The `indicatorStyle`/`indicatorTransition` variables and `updateIndicator` function have been REMOVED. The `theme-style-regression.test.ts` now asserts the CSS-only invariant (no `offsetWidth` measurement, no `indicatorStyle` variable, `border-primary` present on active tab). Do NOT re-introduce JS-based indicator width measurement.
- **v0.9.20 Scoped theme transition CSS (hard boundary)**: `app.css` MUST NOT use a global `*` transition selector for theme swaps. The transition MUST be scoped to a `.theme-changing` class on `<body>` + named-surface selectors (`bg-background`, `bg-card`, `border-border`, `text-foreground`, etc.) for color-only properties (background-color, border-color, color, box-shadow). The `.theme-changing` class is added before `data-theme` swap and removed after 250ms. `applyDarkMode` accepts `skipTransition = false`; onMount passes `true` to avoid first-paint flash. This follows the css-architecture.com best practice and the clash-verge-rev / VS Code pattern. Never re-add `* { transition: ... }` — it transitions thousands of elements (table cells, hidden divs) and causes visible jank.
- **v0.9.20 WebView2 right-click context menu suppression (hard boundary)**: `<svelte:window>` in App.svelte MUST include `on:contextmenu={(e) => e.preventDefault()}` to suppress the WebView2 native right-click menu globally. This is the documented Tauri pattern (clash-verge-rev uses `webview.oncontextmenu = () => false`). Never remove this handler; without it users see "Refresh", "Save as", "Inspect" browser controls in a desktop app.
- **v0.9.20 Theme style i18n keys (hard boundary)**: All 10 locale files (en, zh, ja, ko, de, fr, es, pt, ru, ar) MUST contain `settings.themeStyle`, `settings.themeStyleSlate`, `settings.themeStyleBlue`, `settings.themeStyleViolet`, `settings.themeStyleRose`, `settings.themeStyleCyan`, `settings.themeStyleAmber` keys. When adding a new base color style, add the locale key to all 10 files AND update the theme-style-regression.test.ts `requiredKeys` array. Regression test verifies all 10 locales have all keys.
- **v0.9.20 Theme style persistence + event dispatch (hard boundary)**: App.svelte MUST define `handleThemeStyleChange(e: CustomEvent<string>)` that updates `themeStyle` state, sets `data-theme-style` attribute on `<html>`, and adds `.theme-changing` class for scoped CSS transition. SettingsDialog.svelte `changeThemeStyle` MUST `dispatch('themeStyleChange', style)` instead of direct DOM mutation — the DOM update belongs to App.svelte's handler. App.svelte onMount MUST read `getSetting('themeStyle')` with an allow-list validation (`['slate','blue','violet','rose','cyan','amber'].includes(stored)`) so the persisted theme style survives restart. Never remove the event dispatch; without it the App.svelte state diverges from the SettingsDialog prop and the next toggle reverts to the stale App state. `toggleDarkMode` MUST NOT dispatch `themeStyleChange` — it only dispatches `themeChange`. The two concerns (dark/light vs slate/blue/violet/rose/cyan/amber) are independent.
- **v0.9.20 Tab indicator initial width 0px (hard boundary)**: `indicatorStyle` MUST initialize to `'width: 0px; transform: translateX(0px)'` with no `left` property. The update path uses `transform: translateX` exclusively; a stale `left` value from a prior implementation caused the indicator bar to render wider than the active tab label on first paint. The `indicatorTransition` starts as `'none'` and is set to the cubic-bezier transition after the first successful `updateIndicator` call via `requestAnimationFrame`. Never add `left` to the initial `indicatorStyle` value; never set a non-zero initial width. Regression test: `theme-style-regression.test.ts` asserts `indicatorStyle` matches `/^width:\s*0px/`.
- **v0.9.20 Scoped theme transition CSS (hard boundary)**: `app.css` MUST NOT use a global `*` transition selector for theme swaps. The transition MUST be scoped to a `.theme-changing` class on `<body>` + named-surface selectors (`bg-background`, `bg-card`, `border-border`, `text-foreground`, etc.) for color-only properties (background-color, border-color, color, box-shadow). The `.theme-changing` class is added before `data-theme` swap and removed after 250ms. `applyDarkMode` accepts `skipTransition = false`; onMount passes `true` to avoid first-paint flash. This follows the css-architecture.com best practice and the clash-verge-rev / VS Code pattern. Never re-add `* { transition: ... }` — it transitions thousands of elements (table cells, hidden divs) and causes visible jank.
- **v0.9.20 WebView2 right-click context menu suppression (hard boundary)**: `<svelte:window>` in App.svelte MUST include `on:contextmenu={(e) => e.preventDefault()}` to suppress the WebView2 native right-click menu globally. This is the documented Tauri pattern (clash-verge-rev uses `webview.oncontextmenu = () => false`). Never remove this handler; without it users see "Refresh", "Save as", "Inspect" browser controls in a desktop app.
- **v0.9.20 Theme style i18n keys (hard boundary)**: All 10 locale files (en, zh, ja, ko, de, fr, es, pt, ru, ar) MUST contain `settings.themeStyle`, `settings.themeStyleSlate`, `settings.themeStyleBlue`, `settings.themeStyleViolet`, `settings.themeStyleRose`, `settings.themeStyleCyan`, `settings.themeStyleAmber` keys. When adding a new base color style, add the locale key to all 10 files AND update the theme-style-regression.test.ts `requiredKeys` array. Regression test verifies all 10 locales have all keys.oundary)**: All read APIs (`listVariablesRaw`, `listPathEntries`, `listHistory`, `listProtection`, `listProfiles`) MUST use stale-while-revalidate (SWR) pattern: if stale cache data exists (TTL expired), return it immediately for instant render, then kick off a background refresh that updates the cache silently. Cache TTL raised from 5000ms to 15000ms across all five APIs. `listProfiles` now has a cache (previously un-cached, the largest single source of page latency). `invalidateApiCache()` MUST mark caches stale (set `time = 0`) without clearing data, so SWR returns stale data + background refresh on next read instead of blocking. `ProtectionPage.svelte` MUST launch all 4 IPC calls (`listProtection`, `listVariablesRaw`, `listPathEntries("user")`, `listPathEntries("system")`) concurrently via `Promise.all`, NOT serially await `listProtection()` first. `App.svelte` onMount MUST call `void preloadAdjacentPages()` after the initial `listVariables()` to warm PathEditor + ProtectionPage caches in background. Never regress any read API back to blocking-on-cache-miss; never lower TTL below 15000ms; never clear cached data in `invalidateApiCache` (only set time to 0).
- **v0.9.21 UI aesthetic overhaul — 6-color theme + custom titlebar + CSS containment (hard boundary)**: The frontend dual-axis theme system now supports 6 base colors: `slate` (default), `blue`, `violet`, `rose`, `cyan`, `amber`. The `zinc` and `neutral` options are REMOVED. The theme style selector in `SettingsDialog.svelte` MUST be a `<select>` dropdown (not buttons) that dispatches `themeStyleChange` with the selected value. All 10 locale files MUST contain `settings.themeStyleBlue`, `settings.themeStyleViolet`, `settings.themeStyleRose`, `settings.themeStyleCyan`, `settings.themeStyleAmber` i18n keys. `tauri.conf.json` windows[0] MUST have `"decorations": false` and `"label": "main"`. `capabilities/default.json` MUST include `core:window:allow-close`, `core:window:allow-minimize`, `core:window:allow-toggle-maximize`, `core:window:allow-start-dragging` permissions. `App.svelte` MUST import `getCurrentWindow` from `@tauri-apps/api/window` and render a `.titlebar` div with `data-tauri-drag-region` containing minimize/maximize/close buttons calling `getCurrentWindow().minimize()/toggleMaximize()/close()`. The close button MUST use class `titlebar-btn close` (two classes, matching CSS selector `.titlebar-btn.close:hover`). `app.css` MUST define `.titlebar` (h:32px, bg:hsl(var(--card)), flex), `.titlebar-drag` (app-region:drag), `.titlebar-btn` (w:46px, app-region:no-drag, transition 0.15s), `.titlebar-btn.close:hover` (bg:hsl(var(--destructive))). CSS containment rules `.table-container`, `.list-container`, `.scroll-area` (contain:layout paint) and `.offscreen-optimizable` (content-visibility:auto, contain-intrinsic-size:auto 500px) MUST be present in `app.css`. Components with large scrollable containers (HistoryPage, Variables, ProtectionPage, AuditPage, ServicePage, ProfilePage) MUST add `table-container` or `list-container` class to those containers. Never re-add `zinc` or `neutral` theme styles; never revert to native window decorations; never remove the containment classes from those containers.

- **v0.9.21 Lazy code splitting activation (hard boundary)**: `frontend/src/App.svelte` MUST use dynamic `import()` via `loadComponent(key, importer)` for 6 non-default tabs (ProfilePage, PathEditor, HistoryPage, ProtectionPage, AuditPage, ServicePage). The `lazyComponentCache` Record and `preloadComponent` function MUST be wired to the tab switch logic — the prior v0.8 code had scaffolding (`loadComponent` + `lazyComponentCache` + 6 `lazyXxx` variables) but never called them, making them dead code. Each `{#await loadComponent(...)}` block MUST include a chunk-load-error fallback with a retry button (`delete lazyComponentCache[key]; activeView.set(key)`). `preloadComponent` preloads the adjacent tab 300ms after startup. Variables tab stays static import (first-paint default tab). Vite build MUST produce multiple chunks, not a single monolithic bundle. Never revert to 6 static imports — that defeats code-splitting and bloats first-paint bundle.
- **v0.9.21 Process leak prevention + IPC connection counter (hard boundary)**: `frontend/src-tauri/src/main.rs` MUST track IPC connections via an `AtomicU64` counter. Long-running CLI subprocesses (e.g. `service refresh` with periodic mounts) MUST use `tokio::spawn` with bounded timeout (30s for read, 60s for write). The `run_cli` function MUST drain stdout/stderr via `tokio::io::read_to_end` before the process exits — the prior v0.7 synchronous `ReadToEnd` pattern deadlocked on full pipes (same root cause as v0.7.12 pwsh async drain). Guard: if a child process handle is still alive when the Tauri window is destroyed, the `on_window_event(RequestedClose)` handler MUST waitpid/kill all tracked child PIDs before exit. Never store `std::process::Child` without a drop guard.
- **v0.9.21 Silent performance telemetry (hard boundary)**: `frontend/src/App.svelte` MUST track view-switch timing (from `activeView.set` to first render of the awaited component) and log via `frontendLog("debug", "view_switch {view} {elapsed_ms}")` — this is DEV-ONLY telemetry, NOT user-facing. `run_cli` in `main.rs` MUST log `elapsed_ms` for each CLI invocation at debug level via `log::debug!("[run_cli] {} elapsed_ms={}", args.join(" "), elapsed)`. The telemetry MUST be silent (no toast, no UI widget, no error on failure). Its sole purpose is to give developers diagnostic data when a user reports a performance regression. Never surface telemetry in the GUI; never log at info/warn level (would pollute user logs).
- **v0.9.21 Secret provider timeout + long-session memory safety (hard boundary)**: All `ISecretProvider` implementations MUST enforce a 30-second process timeout for external binary calls (sops, op, pwsh). The CLI `RunPowerShell` / `RunExternalBinary` MUST use `Task.Run` + `Task.WhenAny(task, Task.Delay(30000))` — if the timeout wins, the process is killed and the provider returns a fail-closed error (never a silent default). Long-session memory safety: decrypted secret values in C# MUST use `SecretString` ref struct which zeroes the underlying `char[]` on `Dispose`. The launcher process (`profile launch`) MUST zero the injected env block after `CreateProcess` returns. Never store decrypted material in a `string` — `string` is immutable and cannot be zeroed.
- **v0.9.21 Ponytail-debt ledger (maintenance record)**: 7 deliberate-shortcut comments marked `ponytail:` in the codebase, all on the service/Rust side. These are intentional technical-debt markers, not bugs: (1) `main.rs:454` spawn_blocking keeps `Command` return type; (2) `audit_ledger.rs:33` ProgramData path; (3) `audit_ledger.rs:230` reuse DPAPI rather than aes-gcm crate; (4) `cert_bootstrap.rs:233` service.config.json path; (5) `main.rs:81` best-effort file open; (6) `pipe_dacl.rs:4` one free fn SDDL approach; (7) `process_guard.rs` binary match. Each is a documented decision to not over-engineer: the simplest approach that works and is safe. They MUST NOT be "fixed" by adding abstraction layers unless the underlying requirement changes.
**v0.9.29 ponytail-audit (2026-08-23 supplement):** (a) 8th `ponytail:` marker found: `frontend/src-tauri/src/main.rs:454` (spawn_blocking keeps `Command` return type — intentional, do not refactor) and `scripts/test-secret-string.ps1:4` (self-contained smoke test, no framework — intentional). (b) `bits-ui` and `tailwindcss-animate` are declared runtime deps in `frontend/package.json` with zero source usage — candidate removal; hold until UI design confirms whether Card/Dialog primitive is wanted. (c) `frontend/src/lib/api.ts` at 1,551 lines is approaching junk-drawer territory; consider domain split after regression tests land.
**v0.9.30 signing-prep:** `docs/code-signing-policy.md` added (SignPath Foundation application prerequisite: required phrase, team roles, verifiable-build + privacy statements). `env-manager.csproj` now sets `<Product>Env Manager</Product>` + title/description; `service/Cargo.toml` version drift fixed (0.9.24 -> 0.9.30) to satisfy SignPath same-version artifact metadata rule.


- **v0.9.22 Titlebar position:fixed (hard boundary)**: `app.css` `.titlebar` MUST use `position: fixed; top: 0; left: 0; right: 0; z-index: 100` — NOT `position: relative`. The content wrapper in `App.svelte` MUST use `h-screen pt-[32px] overflow-hidden` to compensate for the 32px fixed titlebar height, and the inner content div MUST use `h-full overflow-y-auto` as the sole scroll container. Prior to v0.9.22, the titlebar was `position: relative` inside the scrollable `.min-h-screen` div, causing it to scroll away when the user scrolled down — making the minimize/maximize/close buttons unreachable. Never regress `.titlebar` to `position: relative`.
- **v0.9.22 Scrollbar auto-hide via native overlay (hard boundary)**: `App.svelte` global scrollbar CSS MUST NOT include `:global(*:hover)::-webkit-scrollbar-thumb` or `:global(*:hover) scrollbar-color` rules. The `:hover` rules forced the scrollbar thumb to stay visible whenever the mouse was over the scroll area, blocking Chromium's native overlay auto-hide behavior (thumb appears on scroll, fades when idle). The fix removes all `:hover` scrollbar rules (both light and dark mode blocks), keeping only the base (idle transparent) and `:active` (dragging highlight) rules. `scroll-behavior: smooth` is added to `:global(body)` for CSS-only smooth scrolling (Baseline since 2022, zero JS). Never re-add `:hover` scrollbar thumb visibility rules.
- **v0.9.22 Header dark mode toggle (hard boundary)**: `App.svelte` header MUST include a Sun/Moon toggle button (from `lucide-svelte`) positioned to the LEFT of the refresh button. The button calls `applyDarkMode(!darkMode)` + `void setSetting('darkMode', String(!darkMode))` on click, and the icon switches between `Sun` (dark mode active, click to switch to light) and `Moon` (light mode active, click to switch to dark). The tooltip uses `$t('settings.lightMode')` / `$t('settings.darkMode')` i18n keys. The header toggle MUST stay in sync with the SettingsDialog toggle — both call the same `applyDarkMode` function and persist via `setSetting`. Never remove the header toggle or let it drift out of sync with SettingsDialog.
- **v0.9.22 Font size range slider (hard boundary)**: `SettingsDialog.svelte` font size control MUST use a native `<input type="range" min={0.8} max={1.5} step={0.05} value={selectedFontScale}>` with a percentage label (`Math.round(selectedFontScale * 100)}%`), NOT a row of preset buttons. The prior 6-button layout (0.85/1/1.15/1.3/1.45/1.6) was cramped and squeezed the CLI-in-PATH toggle below. The modal container MUST use `max-h-[90vh] overflow-y-auto` to prevent content overflow when the user selects a large font size. The slider uses `accent-[hsl(var(--primary))]` for theme-aware coloring. Never regress to preset buttons.
- **v0.9.22 Notes reactivity tracking (hard boundary)**: `Variables.svelte` note tooltip visibility MUST reference `{notesTick}` in the Svelte template `{#if}` condition so Svelte's static reactivity analyzer tracks the state change. The prior code bumped `notesTick++` inside `ensureNotesLoaded()` (an async function), but Svelte does not track store reads or variable assignments inside async function bodies — only references that appear directly in template expressions or `$:` reactive statements. The fix adds `notesTick` to the tooltip condition: `{#if hoveredNoteVar === variable.name && notesTick && getNoteSync(variable.name)}` and uses the comma-expression pattern `(notesTick, getNoteSync(variable.name))` in title/icon conditions to force Svelte to re-evaluate when notesTick changes. The `on:mouseenter` handler also assigns `notesTick = notesTick + 1` after `ensureNotesLoaded().then()` so the tick is a top-level assignment, not buried in an async function's internal increment. Never rely solely on `notesTick++` inside async functions for Svelte reactivity. **[SUPERSEDED by v0.9.23]**: this boundary described the plain-`let` + comma-expression workaround. The v0.9.23 fix replaced `notesTick` entirely with a `writable(0)` store (see v0.9.23 notesTick writable store boundary below), making the comma-expression pattern unnecessary — `$notesTick` auto-subscription handles reactivity. The plain-`let` pattern no longer exists in the codebase.
- **v0.9.22 PathEditor notes shared notesStore (hard boundary)**: `PathEditor.svelte` MUST import `{ loadNotes, getNoteSync, upsertNote }` from `'../notesStore'` and `{ openInputDialog }` from `'../stores'`. The note key format is `path:${scope}:${entry.path.toLowerCase().replace(/\\+$/, '')}` — NOT the variable name key used in Variables.svelte — so path notes and variable notes are independent and never collide. The note button uses `Tag` icon (filled, existing note) vs `FileText` icon (outline, no note), matching the Variables.svelte pattern. The path display area `title=` attribute shows the note content if a note exists, otherwise shows `$t('messages.clickToCopy')`. The same `notesTick` reactivity pattern from Variables.svelte is reused here. Never use a separate notes store or key format that could collide with variable notes.
- **v0.9.22 applyStagedMoves bidirectional + protected skip (hard boundary)**: `PathEditor.svelte` `applyStagedMoves` function MUST include BOTH `while (realPos > i) { movePathEntryUp }` AND `while (realPos < i) { movePathEntryDown }` loops. The prior code only had the move-up loop, so when a user staged a move-down (entry target position > current position) and clicked Apply, the entry could not reach its target via move-up alone — the Apply silently failed for move-down staged moves. The fix adds the second while loop to move entries right (move-down) when their live position is below the target. Protected entries (`target[i].isProtected && realPos !== i`) MUST be skipped during commit with a `frontendLog('warn', ...)` warning — they can be visually reordered in the staged view but must not be moved in the registry. The pure logic is extracted to `src/lib/pathMoveResolver.ts` (`resolveStagedMoves()`) which is tested by `src/tests/pathMoveResolver.test.ts` (7 Vitest tests, no Playwright). Never remove the move-down loop or the protected-entry skip guard.

- **v0.9.23 Titlebar flex layout + content bottom truncation fix (hard boundary)**: `App.svelte` root content div MUST use `class="h-screen flex flex-col overflow-hidden ..."` with the `.titlebar` as `position: relative; flex-shrink: 0; z-index: 100;` — NOT `position: fixed`. The prior `position: fixed` titlebar required a `pt-[32px]` padding compensation on the content area, which caused the total height to be `100vh + 32px`, clipping the bottom 32px of content (last table rows, bottom of modal dialogs). The flex-column layout makes the titlebar occupy its natural 32px height within the flex flow, and `overflow:hidden` on the parent prevents the content area from scrolling beyond the viewport. Never re-add `pt-[32px]` to the content div or `position: fixed` to `.titlebar`.

- **v0.9.23 notesTick writable store — async reactivity fix (hard boundary)**: `notesTick` in both `Variables.svelte` and `PathEditor.svelte` MUST be a Svelte `writable(0)` store, NOT a plain `let` variable. Svelte 4's compiler does NOT track reactive writes (assignments) that happen inside async function bodies after an `await` — the Svelte docs confirm "Values that are read asynchronously — after an `await` or inside a `setTimeout` — will not be tracked." So `notesTick++` inside `handleNote`/`handlePathNote` after `await upsertNote(...)` silently failed to trigger template re-render, making note state appear stale until a manual page refresh. The fix replaces `let notesTick = 0` with `let notesTick = writable(0)` and all `notesTick++` with `notesTick.update(n => n + 1)` — store `.update()` is an explicit notification that bypasses compiler tracking. Template references use `$notesTick` (auto-subscription). Never regress `notesTick` to a plain `let` — the async write path will silently break note display refresh.

- **v0.9.23 PathEditor move-up/down uses display position, not raw index (hard boundary)**: `PathEditor.svelte` move-up/move-down button handlers MUST pass the `pos` parameter (the display position index from `{#each displayEntries as entry, pos}`), NOT `entry.index` (the raw registry index which never changes during staging). The prior code passed `entry.index` to `handleMoveUp`/`handleMoveDown`, so clicking move-up on a staged entry whose visual position was 2 but whose raw index was 1 called move on position 1 instead of position 2 — moving the wrong entry or no-oping. The序号 column (first `<td>`) MUST render `{pos + 1}` (visual 1-based position), NOT `{entry.index}` (raw registry position). The staged entries array preserves `entry.index` as a stable identity key for `applyStagedMoves`, but the displayed index MUST be the visual position. Never pass `entry.index` to move handlers when a staged view is active.

- **v0.9.23 withoutTransition dark mode toggle — no white flash (hard boundary)**: `App.svelte` `applyDarkMode` MUST use the industry-standard `withoutTransition` pattern (mode-watcher / reemus.dev): (1) create a `<style>` element with `* { transition: none !important; }`, (2) append to `document.head`, (3) apply `data-theme` attribute change synchronously, (4) force browser reflow via `void window.getComputedStyle(styleEl).opacity`, (5) remove the `<style>` on the next `requestAnimationFrame`. This eliminates the white flash that occurred on locked/muted elements (which use `text-muted-foreground` tokens) during light→dark transition — the old `body.classList.add('theme-changing')` + `setTimeout(250ms)` approach left CSS transitions active during the color repaint, causing a brief white intermediate state. The `getComputedStyle` call is the critical step: it forces the browser to synchronously evaluate and paint the transition-disabled state before the style element is removed (reemus.dev "ultimate solution"). The previous `.theme-changing` CSS class is removed from `app.css`. Never re-introduce `setTimeout`-based transition disabling or a `.theme-changing` CSS class.

- **v0.9.23 Dark mode --destructive brightness fix (hard boundary)**: All dark mode `--destructive` CSS token values in `app.css` MUST be `0 72.2% 50.6%` (HSL), NOT the prior `0 62.8% 30.6%` which was too dark to read on dark backgrounds. The shadcn/ui issue #281 confirmed this exact contrast problem; shadcn v4's updated dark `--destructive` is `oklch(0.704 0.191 22.216)` ≈ HSL `0 100% 70%`. The chosen `0 72.2% 50.6%` is between Tailwind red-400 (`0 72% 51%`) and the shadcn v4 value, providing readable red text for "Remove dead entries" and other destructive actions in dark mode without being overly bright. Light mode `--destructive: 0 84.2% 60.2%` stays unchanged. Never regress dark mode `--destructive` to `0 62.8% 30.6%` or darker values.

- **v0.9.26 PathEditor move-down guard order (hard boundary)**: `handleMoveDown` in `PathEditor.svelte` MUST call `ensureStagedActive()` BEFORE the position guard `if (pos >= arr.length - 1) return`, NOT after. The prior code placed the guard before `ensureStagedActive()`, so on first click `stagedEntries = []` (length=0, 0-1=-1), `pos >= -1` was always true, and the return blocked the only init entry — making the first move-down click a no-op. The fix swaps the order: init first, guard after (using `arr.length` from the now-populated staged array). `handleMoveUp` keeps its `pos <= 0` guard before `ensureStagedActive()` because `pos <= 0` does not depend on staged state and is safe. Never regress `handleMoveDown` to guard-before-init order — the first click on a fresh page will silently no-op. ADR 0006 (z-index) and ADR 0007 (color-scheme) document the related GUI layer decisions.

- **v0.9.26 PathEditor dead-entries confirm handler same-source (hard boundary)**: `handleRemoveDeadConfirm` in `PathEditor.svelte` MUST read `const deadCount = liveDeadCount` (the reactive `$:` derived value from `displayEntries`), NOT `healthSummary?.dead ?? 0`. The button `disabled` condition already uses `liveDeadCount` (v0.9.25 reactive derived). The prior code read `healthSummary?.dead`, which is only filled by the manual "health check" click and resets to null on every `refresh()`, so the button was lit (liveDeadCount > 0) but the confirm handler read 0 and showed "no dead entries" — a false negative. WCAG 4.1.2 requires disabled state to be programmatically determinable from real data; the confirm handler must use the SAME data source as the button. Never regress the confirm handler to read `healthSummary` — it is a manual-scan artifact decoupled from live `displayEntries`.

- **v0.9.26 GUI z-index layering (hard boundary, ADR 0006)**: `.titlebar` in `app.css` MUST use `z-index: 50` (navbar level per TailwindCSS z-index convention). All overlay surfaces — `ConfirmDialog.svelte`, `InputDialog.svelte`, and the Toast container in `App.svelte` — MUST use `z-[100]`. The prior titlebar `z-index: 100` was greater than Modal/Toast `z-[60]`, suppressing all overlays behind the fixed titlebar. The titlebar must stay above scrolling content (z-50 > content) but must never suppress modal/toast overlays (z-50 < z-[100]). Never regress titlebar to z-100 or overlays to z-[60] — the fixed titlebar at z-100 will block every modal and toast in the app.

- **v0.9.26 Form control color-scheme override (hard boundary, ADR 0007)**: `app.css` MUST include `input[type="radio"], input[type="checkbox"] { accent-color: hsl(var(--primary)); color-scheme: light; }` and `input, select, textarea { color-scheme: light; }` and `input:focus, select:focus, textarea:focus { outline: none; }`. The `:root` `color-scheme: light dark` (v0.9.24 hard boundary for WebView2 native scrollbar) inherits to all elements, making native form controls follow the Windows system `prefers-color-scheme` instead of the app `data-theme`. The element-level `color-scheme: light` overrides `:root` for form controls only, binding fill/focus to app theme variables while leaving the scrollbar (which only reads :root) following `data-theme`. `accent-color` binds radio/checkbox fill to app `--primary` (MDN Baseline: Edge/Chrome/Firefox/Safari all support). `outline: none` removes the system black focus border so existing Tailwind `focus:ring-1 focus:ring-primary` takes over (shadcn/VS Code standard). Never regress these rules — without them, radio/checkbox/select/input focus will follow Windows system dark mode instead of the app theme on hosts where `prefers-color-scheme: dark` is set at the OS level.


