# Hard Boundaries (Red Lines)

> **Agent instruction layer** — not user documentation.
> Extracted from AGENTS.md for progressive disclosure (industry best practice:
> root AGENTS.md <32 KiB, domain-specific rules in docs/agents/*.md).
> Source: [AGENTS.md](../../AGENTS.md) canonical reference.

This document lists all invariants that must never be violated by any code change.

These invariants must never be violated by any code change:

- **Protected variables**: built-in protected system variables are seeded from embedded `protection.defaults.json` into `%LOCALAPPDATA%\EnvManager\builtin-protected-vars.json`. They cannot be set, deleted, toggled, renamed, scope-changed, or added to a profile at system scope. Custom user-locked variables (`protected-vars.json`) cannot be toggled, edited, or deleted until unlocked. See `IsProtectedVariable`.
- **Protected PATH entries**: built-in defaults are seeded from embedded `protection.defaults.json` into `builtin-protected-paths.json`. Built-in and custom locked entries (`protected-paths.json`) cannot be removed or edited via `path remove`/`rename`. See `IsProtectedPathEntry`. Reordering (`move-up`/`move-down`) is allowed because it is non-destructive.
- **Cross-process mutex**: all write operations acquire `Local\EnvManager.RegistryMutation` mutex. Plus Rust `CLI_RWLOCK` write lock. Plus frontend `writeChain` serialization. Three layers, never bypass.
- **Variable rename/scope-change contract**: `rename` writes+verifies target before deleting source. `change-scope` writes new scope, verifies, deletes source, relocates `_EnvManager_disabled` backup. Both reject protected `oldName`/`newName`/source-scope/target-scope at entry point. Never delete-then-set for renames.
- **GUI EditDialog 3-way save ordering**: `rename(old scope)` -> `changeScope(overwrite flag, never hardcoded true)` -> `setVariable(value, overwrite flag)`. The `--overwrite` flag flows only from explicit user confirmation of the conflict modal, never an injected synthetic `true`.
- **Profile audit**: `TryUndoProfileAudit` uses allow-list of known subcommands plus Id-based conflict detection; unknown `profile <x>` subcommands emit error and `return false` (never silently succeed); try/catch fallback returns false so `--force` contract works.
- **v0.7.0 Launch profiles + DPAPI secrets**: a Launch profile is NEVER written to the registry, NEVER broadcasts `WM_SETTINGCHANGE`. `profile launch` spawns the target with `env_clear` + inject, classified as read in Rust `is_read_only`. `profile set-launch` is a write (mutates profiles.json, holds `CLI_RWLOCK` write lock). `ValidateLaunchTarget` rejects `\Windows\System32` targets and non-executable extensions. Profile names are globally unique because all CLI profile commands are name-addressed.
- **v0.7.0 DPAPI secrets (hard boundary)**: secret variable values are DPAPI-CurrentUser encrypted on disk (base64 of `CryptProtectData` output, scope CurrentUser). Plaintext lives only in transient CLI/launcher process memory - never written to `profiles.json`, the registry, or logs. `profile reveal-secret` is the ONLY stdout-plaintext path and is DPAPI-bound to the current user (cannot be decrypted by another user or machine). `profile launch` decrypts secrets in the launcher process; if decryption fails the launch is refused (never silently inject ciphertext garbage). Audit records the variable NAME plus `<redacted>`/`<encrypted>` markers - never the plaintext or ciphertext. `add-secret`/`edit-secret` require the profile to be unapplied (same as other profile mutations).
- **v0.7.0 Secrets never applied to registry (hard boundary)**: `IsProfileApplicable` rejects any profile containing a secret variable from being applied to the user registry. Secrets on a Global profile can be stored but NEVER applied - applying would write DPAPI ciphertext garbage to the registry, violating the plaintext-never-persisted invariant. Secrets are meaningful only on Launch profiles (env_clear + inject + decrypt-in-process). `ProfileEditSecret` rejects renaming a secret into a protected system variable name (same entry-point invariant as `ProfileAddSecret`).
- **v0.8 Versioned Secret Envelopes (hard boundary)**: All secret values written to profiles.json are wrapped in a JSON envelope { provider, version, createdAt, ciphertext } for dpapi-current-user or { provider, version, targetName } for credential-manager. SecretProviderManager routes to the active provider declared in secret-providers.json. Unknown providers are fail-closed. Bare pre-v0.8 DPAPI base64 blobs are auto-detected and decrypted via DpapiCurrentUserProvider. CredentialManagerProvider uses advapi32.dll CredWriteW/CredReadW/CredDeleteW with CRED_TYPE_GENERIC; the credential blob is DPAPI-encrypted before CredMan storage. Profile stores only the CRED target name. CLI command profile secret-provider list (read) and set (write) manages the active provider. Audit records provider name, never plaintext or ciphertext.
- **v0.8 Phase 3 Secret Rotation/Export/Import (hard boundary)**: `profile secret-provider rotate` re-encrypts all secrets across all profiles with the active provider. If any secret fails to decrypt (wrong provider, deleted CredMan entry), the rotation skips it and increments the failed count - the failed secret is NOT deleted. The `profile export-secrets` command exports secrets to a DPAPI-CurrentUser-encrypted backup file (portable within the same user account). The `profile import-secrets` command imports secrets from an encrypted backup; each imported secret is verified by trial-decryption before being written to the profile. Import requires the profile to be unapplied. Rotation/export/import all generate audit entries with operation name, file path, and counts - never the secret values. Rotation = write (mutates profiles.json); export = read; import = write.
- **v0.8 Phase 4-5 Additional Secret Providers (hard boundary)**: Two new ISecretProvider implementations are registered in SecretProviderManager:

- **PowerShellSecretManagementProvider** (name: powershell-secretmanagement): delegates encryption to PowerShell Set-Secret and decryption to Get-Secret via a hosted pwsh process with CREATE_NO_WINDOW. The profile stores only the vault name and secret name in the envelope TargetName; the actual secret value lives in the PowerShell SecretManagement vault. Requires PowerShell 7 + Microsoft.SecretManagement + Microsoft.SecretStore modules installed. Fail-closed if pwsh is not found or exits non-zero. The 30-second process timeout prevents indefinite hangs.

- **VaultKV2Provider** (name: vault-kv2): reads/writes secrets via the Vault HTTP API (POST /v1/secret/data/<path> and GET /v1/secret/data/<path>). The profile stores only the mount path, secret path, and key name in the envelope TargetName. Token is read from VAULT_TOKEN env var (never persisted). TLS is mandatory: VAULT_ADDR must use https:// for non-localhost addresses; http:// is only permitted for 127.0.0.1/localhost/[::1]. Fail-closed on network errors, 403/404, or timeout (10s). The CLI does not cache decrypted material beyond the launcher process lifetime.



- **v0.7.2 Phase 6-7 SOPS + Azure Key Vault (hard boundary)**: Two new ISecretProvider implementations are registered in SecretProviderManager:

- **SopsProvider** (name: sops): shells out to a verified sops binary (-e/-d) under CREATE_NO_WINDOW with 30s timeout. The profile stores the full sops-encrypted JSON as the envelope ciphertext field. Supports Age, PGP, AWS KMS, Azure Key Vault, GCP KMS, and HashiCorp Vault decryptors via sops env vars. Binary is discovered via SOPS_PATH env var, PATH search, or common install locations. Fail-closed if sops binary is missing or non-functional. Temp files are created in a per-operation isolated directory and securely cleaned up in a finally block. The envelope is self-contained; Delete is a no-op.

- **AzureKeyVaultProvider** (name: azure-keyvault): calls Azure Key Vault REST API (PUT/GET /secrets/<name>?api-version=7.4). Profile stores only vault URI + secret name as TargetName (format: vaultUri|secretName). TLS mandatory (HTTPS only). Token obtained via managed identity (IMDS 169.254.169.254) or service principal (AZURE_CLIENT_ID/AZURE_CLIENT_SECRET/AZURE_TENANT_ID). Token cached in process memory only with 5-minute expiry buffer. 15s HTTP timeout. Fail-closed on 403/404. Supports rotation. Delete issues a soft-delete. Secret names sanitized to alphanumeric + hyphens, max 127 chars.


- **v0.7.3 Phase 8-9 1Password + AWS Secrets Manager (hard boundary)**: Two new ISecretProvider implementations:

- **OnePasswordProvider** (name: 1password): shells out to the 1Password CLI (op) binary via CREATE_NO_WINDOW with 30s timeout. Profile stores vault name + item ID + field name as TargetName (format: vault|itemId|field). Binary discovered via OP_PATH env var, PATH search, or common install locations. Fail-closed if op binary missing. Supports rotation (delete + recreate). Requires OP_ACCOUNT or OP_SERVICE_ACCOUNT_TOKEN for auth. Delete archives the 1Password item.

- **AwsSecretsManagerProvider** (name: aws-secretsmanager): calls AWS Secrets Manager REST API with SigV4 signed requests (AWS4-HMAC-SHA256). Profile stores region + secret ID as TargetName (format: region|secretId). TLS mandatory (HTTPS to secretsmanager.<region>.amazonaws.com). Auth via AWS_ACCESS_KEY_ID + AWS_SECRET_ACCESS_KEY + optional AWS_SESSION_TOKEN env vars. 15s HTTP timeout. Supports rotation via PutSecretValue. Delete uses ForceDeleteWithoutRecovery. Secret IDs sanitized to alphanumeric + /_+=.@- max 512 chars. Full SigV4 canonical request, string-to-sign, HMAC-SHA256 signing key chain implemented in-process (no AWS SDK dependency).


- **v0.7.3 GUI Secret Provider Selector (hard boundary)**: ProfilePage.svelte dynamically loads all available secret providers from `profile secret-provider list` CLI output (no hardcoded provider list). The provider selector uses a `<select>` dropdown that shows all 8 providers with i18n-localized names via `providerDisplayName()`. Clicking the provider badge toggles into selector mode; selecting a provider calls `secretProviderSet(name)` and closes the selector. The `availableProviders` array is populated from CLI output, not from a static array. When adding a new secret provider, add its i18n key to `providerDisplayName()` map in ProfilePage.svelte and all 10 translation files. **GUI/CLI sync rule**: the GUI must never hardcode a provider list; it must always derive from the CLI `secret-provider list` output so new providers appear without UI code changes.

- **v0.7.4 Provider-change confirmation modal (hard boundary)**: the GUI provider `<select>` MUST NOT call `secretProviderSet` directly. It calls `requestChangeProvider` (sets `pendingProvider` and reverts the visible `<select>` value to `activeProvider`), then a confirm modal with `secrets.providerChangeTitle` / `secrets.providerChangeWarning` / `secrets.confirmChange` i18n strings gates the actual `secretProviderSet` call via `confirmChangeProvider`. `cancelChangeProvider` clears `pendingProvider` without side effects. This prevents silent re-encryption-provider swaps when the profile already contains secrets. The CLI `profile secret-provider set` path prints an explicit warning about existing secrets keeping their previous provider's decryption (fail-closed on unknown provider) — no silent cross-provider migration occurs.

- **v0.7.4 PowerShell -EncodedCommand (hard boundary)**: `PowerShellSecretManagementProvider.RunPowerShell` MUST use `pwsh -EncodedCommand <base64(UTF-16LE)>` instead of `-Command "<escaped script>"`. The prior `-Command` path doubled single quotes inside the script then wrapped the whole script in outer `"`, which produced `''Stop''` tokens that pwsh parsed as a `ParserError: Unexpected token 'Stop''`. `-EncodedCommand` (Microsoft-recommended) eliminates all shell quoting. `EscapeForPowerShell` is still used for single-quoted strings INSIDE the script body, but the script itself is never passed through a shell quoting layer.

- **v0.7.4 Regular/secret variable display split (hard boundary)**: ProfilePage.svelte MUST render `profile.variables` in two visually separated sections — "Regular variables" (i18n `profiles.regularVariables`) and "Secret variables" (i18n `profiles.secretVariables`, amber styling with lock icon). A variable is secret iff `selectedProfile?.secretVariables?.includes(pv.name)`. `profile.secretVariables` is the authoritative list; never infer secret-ness from value patterns. The secret block shows `<encrypted>` for the value (never the ciphertext). This separation keeps the audit trail visible and prevents accidental edits to secret-bearing variables.

- **v0.7.4 Global profiles cannot hold secrets (decision)**: Global profiles are prohibited from holding secret variables. The CLI `IsProfileApplicable` already rejects any profile containing `SecretVariables` from being applied to the user registry; the GUI additionally gates the secret provider UI behind `profile.profileType === 'launch' && !profile.isEnabled`. Rationale: secrets on a Global profile could only ever leak ciphertext garbage to the registry on apply (no plaintext path exists for Global profiles), so storing them there is pure risk with no benefit. Users wanting secrets MUST use a Launch profile. The i18n key `profiles.secretsGlobalDisabled` documents this in the UI.

- **v0.7.4 Launch-target / Vault CLI error i18n (frontend only)**: CLI error messages remain English (CLI is locale-neutral per AGENTS.md); the GUI `localizeError` function in ProfilePage.svelte maps `ValidateLaunchTarget` messages (empty / not found / wrong extension / System32 rejection) and `VaultKV2Provider` messages (`VAULT_ADDR environment variable not set` / `VAULT_ADDR must use https://`) to i18n keys `errors.launchTargetEmpty`, `errors.launchTargetMissing` (`{path}`), `errors.launchTargetInvalidExt` (`{ext}`), `errors.launchTargetSystem32`, `errors.vaultAddrNotSet`, `errors.vaultTlsRequired`. When a new CLI error message is introduced, add a regex branch + i18n key here; never let an English CLI string surface to the GUI verbatim.

- **v0.7.4 Microsoft YaHei UI font (hard boundary)**: App.svelte global font-family MUST lead with "'Microsoft YaHei UI', 'Segoe UI', ..." so Chinese/CJK users get the native Microsoft YaHei UI face on Windows. The font stack still falls back to Segoe UI / system-ui / sans-serif for non-CJK locales and macOS/Linux. Never remove 'Microsoft YaHei UI' from the stack; it is the canonical Windows CJK UI font.

- **v0.7.4 HistoryPage column-resize highlight fix (hard boundary)**: `HistoryPage.svelte` column-resize MUST use a `col-resizing` class on the dragged `<th>` plus a `col-resizing-active` class on the table root to pin the highlight indicator to the dragged column head. The resize hit area (`th.resize::after`) is 6px wide with `pointer-events: auto` and `z-index: 2` so hover/click stays inside the current `<th>` and does not jump to the adjacent column when dragging rightward. Without this pin, the blue indicator jumped to the right neighbour on right-drag even though the left column was being resized. `endResize` MUST remove both classes.

- **v0.7.4 Clone-from-existing combobox (hard boundary)**: The "clone from existing variable" picker in ProfilePage's add-var panel MUST be a self-rendered combobox (`<input>` + `<ul>` dropdown), NOT a native `<select>`. The native `<select>` dropdown is OS-controlled, so typing into a separate search `<input>` above it has no visual feedback (the dropdown list does not visibly re-filter inline). The combobox: `on:input` reopens the dropdown and resets `cloneHighlightIndex`; ArrowDown/Up moves highlight within the first 10 results; Enter selects; Esc closes; `on:mousedown` on an item (with `preventDefault` to keep focus) calls `handleCloneSelect` and clears the search. `on:blur` closes after a 150ms delay so click-through works. Each list item shows the variable name (mono) plus a greyed value preview truncated to 40% width.

- **v0.7.5 PowerShell SecretManagement preflight + CLIXML stripper (hard boundary)**: `PowerShellSecretManagementProvider` MUST call `EnsureSecretManagementAvailable()` (probe `Get-Module -ListAvailable *SecretManagement*` — wildcard so it matches BOTH the historical short name `Microsoft.SecretManagement` AND the canonical PowerShell Gallery package name `Microsoft.PowerShell.SecretManagement`; a literal `Get-Module -ListAvailable Microsoft.SecretManagement` returns zero even when the module is correctly installed, so it MUST NOT be used) and `EnsureVaultRegistered()` (probe `Get-SecretVault EnvManager`, auto-`Register-SecretVault -ModuleName Microsoft.SecretStore -AllowClobber`) before any `Set-Secret`/`Get-Secret`/`Remove-Secret` call. This turns the catastrophic `#< CLIXML <Objs ...><S S="Error">Set-Secret is not recognized...</S></Objs>` into a clear actionable message that tells the user to install the canonical PowerShell Gallery module names `Microsoft.PowerShell.SecretManagement` and `Microsoft.PowerShell.SecretStore` (never the short sted names). All pwsh stderr is unwrapped by `StripClixml` before being thrown (parses `<S S="Error">` elements, restores `_x001B_/_x000D_/_x000A_/_x0009_` escapes, strips ANSI color sequences). The `EnvManager` vault is created on first use so users do not need to register it manually. This lives entirely inside `Encrypt`/`Decrypt`/`Delete`; no provider-level state crosses the boundary.
- **v0.7.10 Secret provider errors shown inline (hard boundary)**: `ProfilePage.svelte` MUST surface secret-provider failures (activation probe failure on `secretProviderSet`, and `profileAddSecret` failures) via the `providerErrorMessage` reactive variable that renders an inline amber banner directly under the provider `<select>` (the `secrets.activeProvider` block), NOT via `showMessage(...,'error')` toast. Rationale: provider activation errors carry long actionable environment-fix instructions (Install-Module commands, `VAULT_ADDR` setup, cloud credential setup) that a transient toast truncates and destroys before the user can act on them. The inline banner is dismissable via a Close button that sets `providerErrorMessage = null`. On a successful `secretProviderSet` the banner is also cleared (`providerErrorMessage = null`). Never route a provider error to BOTH the inline banner AND a toast (duplicate noise); the inline banner is the single surface for provider activation errors. Other (non-provider) profile errors continue to use `showMessage` toasts as before.
- **v0.7.10 i18n locale race eliminated (hard boundary)**: `frontend/src/main.ts` MUST `await applyPersistedLocale()` between `setupI18n()` and the `const app = new App(...)` mount. Prior to v0.7.10, `setupI18n()` ran in `main.ts` (synchronously setting the default `en` locale), but `applyPersistedLocale()` (the durable IPC read of the user's persisted locale from `gui-settings.json`) only ran later inside `App.svelte` `onMount`. The result was a first-paint flash showing English UI for ~10-50ms before the async `getSetting('locale')` resolved and re-rendered with the user's persisted locale. The fix mounts App AFTER the durable locale is resolved so the very first Svelte render already uses the user's persisted locale. The `await` is a top-level await (Vite `build.target: 'es2022'` is the minimum that supports it; was `es2021` before v0.7.10 and is now `es2022` in `frontend/vite.config.ts`; do NOT lower it back to es2021 or top-level await will fail the vite build with `Top-level await is not available in the configured target environment ("es2021")`). WebView2 (Chromium-based, preinstalled on Windows 11, available on Windows 10 21H2+) supports es2022 fully. This supersedes the v0.7.6 "setupI18n MUST NOT read localStorage" hard boundary — both are now correct together: setupI18n still does NOT read localStorage, AND main.ts blocks on the durable IPC read. The `await` happening before the error handlers install is acceptable because a failing `applyPersistedLocale` is caught by the same try/catch around setupI18n and falls through to the App mount with the default `en` locale (already set synchronously by setupI18n via `init({initialLocale:'en'})`).

- **v0.7.5 Secret provider activation preflight (hard boundary)**: `SecretProviderManager.SetActiveProvider(name)` MUST run a no-op probe Encrypt/Decrypt/Delete round-trip under the new provider BEFORE committing the config file change. A provider that throws on the probe (pwsh missing module, Vault no `VAULT_ADDR`, cloud credentials missing, network down) is REJECTED at config time with `Cannot activate provider '<name>': <upstream message>. Fix the provider environment first...`. This is the same pattern PowerToys uses to validate extension dependencies at config time, not at use time. The user no longer gets a silent "active provider set to foo" success followed by a CLIXML catastrophe on the next `add-secret`; the failure surfaces where the user can actually act on it. The probe uses an unmistakable sentinel context (`__env_manager_compat_probe__`) so it cannot collide with a real variable name; best-effort Delete swallows provider-specific side effects.

- **v0.7.5 Non-launch profile secret rejection (hard boundary, CLI side)**: `ProfileAddSecret` and `ProfileEditSecret` MUST reject profiles whose `ProfileType` is not `launch` at entry point, before any encryption is performed. The error directs the user to `profile set-launch <name> --target <exe>` to convert a Global profile to Launch first. The GUI side is already gated by `profile.profileType === 'launch' && !profile.isEnabled` for the secret UI; this closes the CLI-side gap so a script or LLM cannot bypass the GUI restriction and create an inert secret on a Global profile that `IsProfileApplicable` would later refuse to apply anyway. Rationale is unchanged from v0.7.4: a secret on a Global profile can only ever write DPAPI ciphertext garbage to the registry on apply, so storing it there is pure risk with no benefit.

- **v0.7.5 Inheritance cycle / self-inheritance rejection (hard boundary)**: `ProfileSetInherits` MUST call `HasInheritanceCycle(target, requestedParents, allProfiles)` (DFS over the existing `Inherits` chains) AND refuse `targetName` appearing in the requested parents before mutating `profile.Inherits`. A cycle (A inherits B which inherits A) or a self-loop (A inherits A) would make `ResolveProfileVariables`/`ResolveProfilePaths` infinite-loop and the profile un-recoverable; rejecting at entry prevents a poisoned graph from being serialized to `profiles.json` and crashing every subsequent load. The DFS uses a visited-set so it survives diamond inheritance without false-negatives.

- **v0.7.5 Profile type filter (GUI)**: ProfilePage.svelte renders a segmented All / Global / Local control above the profile list. The filter is a view-only filter (no CLI call, no profile mutation); the choice is persisted in `localStorage` as `profileTypeFilter` so the user's last filter survives re-opening the GUI. `filteredProfiles` replaces `profileList` in the `{#each}` loop.

- **v0.7.5 Add-var / add-path GUI input validation (hard boundary)**: the GUI add-var/add-path panels MUST run `validateVarNameInput` (rejects `=, NUL, CR, LF, TAB`, length > 255) and `validatePathInput` (rejects `;`, `\0`, `\r`, `\n`, length > 32767) BEFORE IPC. The CLI already has `ValidatePathFragment` and the variable-name guard as defense-in-depth, but failing on the GUI layer keeps the IPC surface clean and gives the user a snappy i18n-localized error (`errors.varNameInvalid`/`errors.pathInvalidChars`/`errors.varNameTooLong`/`errors.pathTooLong`) instead of a raw CLI stderr echo. Never bypass these validations - they protect against the exact PATH-injection-by-semicolon and `NUL`-byte-burying attack vectors the CLI guards against.

- **v0.7.5 History page operation label full-command-first lookup (hard boundary)**: `HistoryPage.svelte`'s `getOperationLabel(command)` MUST try `history.op.<full command>` (e.g. `history.op.path add`) FIRST, then fall back to `history.op.<leading word>` (e.g. `history.op.path`), then return the raw command string. The previous code sliced `entry.command.split(' ')[0]` upstream and lost the subcommand, so `path add`/`path remove`/`path move-up`/`path move-down` all collapsed to the generic "Path" label and `history undo`/`history delete` fell back to plain English. The fix restores subroutine-level granularity for the user-visible History table.

- **v0.7.5 GUI DPI-aware glyph rendering (hard boundary)**: `App.svelte` global CSS MUST include `text-rendering: optimizeLegibility`, `font-feature-settings: "kern" 1, "liga" 1, "calt" 1`, and `-webkit-text-size-adjust: 100%` in addition to the existing 'Microsoft YaHei UI' first font stack. These four together fix the "fuzzy on zoom, blurry on 4K Windows scaling" complaint by enabling TrueType hinting, kerning, common ligatures, and contextual alternates in the Tauri WebView2 host. This is the cc-switch + PowerToys pattern ported wholesale - no new dependency, no font swap.

- **v0.7.6 Secret provider activation error i18n (hard boundary)**: GUI `localizeError` in ProfilePage.svelte MUST match the CLI activation message `Cannot activate provider '<name>': <upstream>. Fix the provider environment first ...` BEFORE falling through to raw CLI text. The match (a) extracts the provider name from the quoted segment, (b) routes `<upstream>` to the provider-specific i18n key (`errors.activate.pwsh` / `errors.activate.sops` / `errors.activate.azure` / `errors.activate.op` / `errors.activate.aws` / `errors.activate.vaultAddr` / `errors.activate.vaultTls` / `errors.activate.vaultToken`), (c) falls back to `errors.activate.generic` with `{upstream}` placeholder if the upstream does not match any known pattern, and (d) returns a single localized message via the `errors.activateProvider` template `{name} + {reason}`. The provider name in the template is rendered through `providerDisplayNameFn()` (a thin alias for the existing `providerDisplayName()`) so the same localized label used by the provider selector is reused here. Native runtime Vault errors (Decrypt at launch time, NOT ActivationError) keep the original two-line `errors.vaultAddrNotSet` / `errors.vaultTlsRequired` mapping. Adding a new secret provider means: add an `errors.activate.<provider>` i18n key to all 10 locales, add a regex branch in `localizeError`, and document the activation-error surface in this hard-boundary ledger.

- **v0.7.6 History operation label i18n call-site (hard boundary)**: HistoryPage.svelte MUST pass `entry.command` (the full audit command string) into `getOperationLabel`, NOT `entry.command.split(' ')[0]`. The `getOperationLabel` function does full-then-head fallback itself (`history.op.<full command>` before `history.op.<leading word>`), so slicing at the call site discards the subcommand and re-introduces the v0.7.4 regression where `path add` / `path remove` / `path move-up` / `path move-down` collapse to "Path" and `history undo` / `history delete` fall back to English. The fix is one line at the call site. Any new audit-recorded command must (a) add a `history.op.<full command>` key to all 10 locales AND (b) verify the GUI label renders correctly under both English and a non-English locale before merge.
- **v0.7.7 Inheritance chain secret propagation (hard boundary)**: `IsProfileApplicable` in `src/ProfileEffective.cs` MUST collect the union of `SecretVariables` across the ENTIRE inheritance chain (via `CollectInheritedSecrets`), not just `profile.SecretVariables` on the profile under test. The prior v0.7 check only walked the profile's own list, so a Global profile that inherited a Launch profile with `secretVariables=['OPENAI_API_KEY']` silently passed apply validation and would write DPAPI ciphertext garbage for `OPENAI_API_KEY` to `HKCU\Environment`. The fix rejects apply when ANY inherited variable name matches ANY inherited profile's secret list. The walk uses a visited set keyed by profile Name so a poisoned profiles.json with an undetected cycle cannot infinite-loop. `CollectInheritedSecrets` is defined alongside `IsProfileApplicable` and is the authoritative source for inherited-secret membership; do not add new ad-hoc walk functions.
- **v0.7.7 Global-inherits-Launch topology rejection (hard boundary)**: `ProfileSetInherits` in `src/ProfileCommand.cs` MUST reject three inheritance combinations at set time, not just at apply time: (a) a Global profile inheriting any Launch profile (`Error: A Global profile cannot inherit from a Launch profile...`), (b) a Launch profile inheriting another Launch profile that already carries secrets (`Error: A Launch profile cannot inherit from another Launch profile that already carries secrets...`), and (c) self-inheritance (v0.7.5 guard retained). The Global<-Launch rejection applies unconditionally because a Launch profile may later be changed to add secrets, and once a Global is applied the inherited ciphertext would silently start leaking. The Launch<-Launch rejection only fires when the parent Launch profile has `secretVariables.Count > 0`, because an empty Launch parent is safe. These checks run BEFORE the new chain is saved to `profiles.json` so a user sees the rejection immediately. If a profiles.json is poisoned (hand-edited cycle), set-inherits wraps ResolveProfileVariables / ResolveProfilePaths in try/catch, prints a clear InvalidDataException message, and returns exit 1 without persisting -- the command itself no longer bricks.
- **v0.7.7 GUI inherits-checkbox combinatorial block (hard boundary)**: `ProfilePage.svelte` inherits checkbox MUST be `disabled` and visually de-emphasized (opacity-40 + cursor-not-allowed) when the combination is blocked: target is `global` + candidate is `launch`, OR target is `launch` + candidate is `launch` + candidate has `secretVariables?.length > 0`. The tooltip MUST show `$t('errors.inheritBlocked')`. A `launch` badge (amber styling) is rendered next to launch-type candidates so the user sees the topology at a glance. The GUI rejects at click time so the user is not forced into a CLI round-trip to learn the rejection; the CLI rejects a second time if the GUI contract is bypassed (e.g. direct CLI invocation). When a new inheritance-blocking rule is added, update GUI combinatorial predicate + CLI injection point + i18n key for all 10 locales + this ledger entry.
- **v0.7.7 Live CLI inheritance-protection integration test (hard boundary)**: any change to `ProfileSetInherits`, `IsProfileApplicable`, `CollectInheritedSecrets`, or the v0.7.7 hard boundaries above MUST be validated by running `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/test-inheritance-protection.ps1`. The script backs up `%LOCALAPPDATA%\EnvManager\profiles.json`, creates four `EM_INHERIT_TEST_*` profiles (global, launch+secret, launch-plain, second global), verifies four cases (global<-launch_secret rejected, launch<-launch_secret rejected, global<-global_accepted, self<-self rejected), cleans up the test profiles, and restores the original profiles.json. The script does NOT mutate the registry. Default CLI exe path is `bin\Release\net10.0-windows\env-manager-cli.exe`; falls back to Debug builds if Release is absent. The previous default `bin\Release\net10.0\env-manager-cli.exe` (pre-multi-target) is intentionally NOT tried because it is a stale 7/11 v0.3.0 build that prints `Unknown command: profile`. The xUnit lane `ProfileSeamValidationTests` (architecture-recovery issue 04) unit-tests the same v0.7.7 rejections against `InMemoryScope` plus a hermetic profiles.json redirect, including a falsifiable launch-inherits-secret-launch poisoned-JSON variant; the live CLI script above remains the end-to-end backstop.

- **v0.7.7 Single-instance GUI (hard boundary)**: `main.rs` MUST register `tauri_plugin_single_instance` as the first plugin after `tauri_plugin_dialog::init()`. The plugin's callback calls `restore_window(app)` so a second launch of `env-manager.exe` restores and focuses the existing window instead of opening a duplicate. Single-instance is enforced at the OS/process level (named mutex), not at the JS layer, so it cannot be bypassed by a stray window-open call. The Cargo dependency is `tauri-plugin-single-instance = "2.0"` (must match the Tauri 2.x line; pin to 2.x, do not jump to a hypothetical 3.x without testing the callback ABI). When upgrading Tauri, verify the single-instance plugin's callback signature is still `Fn(&AppHandle, Vec<String>, String)` — a signature change would silently compile but never fire the restore on second launch.
- **v0.7.7 Settings persistence on startup (hard boundary)**: `App.svelte` onMount MUST read `darkMode` and `fontScale` from `localStorage` and apply them BEFORE the first variable-list fetch. The settings ARE persisted correctly by `SettingsDialog.svelte` (it writes to `localStorage.setItem('darkMode', ...)` / `setFontScale`), but prior to v0.7.7 the App component never read them back on startup, so every relaunch reset dark mode and font size to defaults. The locale is already read back by `i18n.ts`'s `setupI18n()` (it reads `localStorage.getItem('locale')` and async-switches), so locale persistence was never broken; only darkMode and fontScale were. When adding a new persisted setting, add the read-back in the SAME onMount block in App.svelte — never leave a setting that is written but not read back, that is the exact pattern that caused this regression.
- **v0.7.7 Settings persistence via Rust IPC (hard boundary, supersedes the localStorage-only description above)**: WebView2 localStorage is unreliable in the portable build (the tauri-plugin-store file `gui-settings.json` was never created on disk, confirmed by inspecting every app-data directory). GUI settings (locale, darkMode, fontScale) MUST be persisted via two Rust IPC commands declared in `main.rs`: `read_gui_setting(key: String) -> serde_json::Value` and `write_gui_setting(key: String, value: String) -> bool`. These read/write `%LOCALAPPDATA%\EnvManager\gui-settings.json` directly via `std::fs` + `serde_json`, the SAME proven path used by `profiles.json` / `audit.json`. `frontend/src/lib/settingsStore.ts` wraps `invoke('read_gui_setting')` / `invoke('write_gui_setting')` with a localStorage fallback (localStorage is a sync-read instant cache, not the durable store). `setSetting` writes BOTH localStorage (for instant first-render) AND the IPC file (for durability across restarts). `setupI18n` in `i18n.ts` MUST NOT use `getLocaleFromNavigator()` as a fallback in the first-resolve path: on Chinese Windows the navigator returns `zh-CN`, which overwrites the user's explicit `en` choice when localStorage is empty/unflushed; first-resolve uses `stored || defaultLocale` only, and the async IPC store correction (which reads the real persisted file) wins over stale localStorage. Never re-introduce `tauri-plugin-store` (removed from Cargo.toml, main.rs, package.json) - it did not create the file in the portable build. Never re-introduce `getLocaleFromNavigator` in the first-resolve path. When adding a new persisted GUI setting: add a key, read via `getSetting(key)` in `App.svelte` onMount BEFORE the first fetch, write via `setSetting(key, value)` in the settings dialog handler so both layers stay in sync. ADDITIONALLY (v0.7.7 locale regression fix): `setupI18n` in `i18n.ts` MUST NOT call `setSetting('locale', resolved)` at startup. The prior bug was that setupI18n unconditionally wrote the sync-resolved locale (stale localStorage or defaultLocale) to the durable IPC file BEFORE the async `getSetting('locale')` could read and protect the user's actual persisted choice. This caused the user's explicit `en` preference to be overwritten by `zh` (stale localStorage) on every restart. The fix: setupI18n writes to localStorage sync (for first render), but the IPC file is only written by `SettingsDialog.switchLocale` (user explicit action) or by the async-correction path when the IPC file is empty but localStorage has a value (first-run seeding). The IPC `getSetting('locale')` is the SINGLE SOURCE OF TRUTH and authoritative over stale localStorage after init. Never re-introduce an unconditional `setSetting` in the startup path.
- **v0.7.7 History action column reactive $t (hard boundary)**: `HistoryPage.svelte`'s `getOperationLabel` MUST accept the translation function as a parameter (`tFn: (key: string) => string`) and the template call site MUST pass `$t` explicitly: `{getOperationLabel(entry.command, $t)}`. The prior v0.7.6 fix only corrected the call-site argument (`entry.command` instead of `entry.command.split(' ')[0]`), but kept `$t` INSIDE the function body. Svelte's static reactivity analyzer does NOT track `$store` reads inside function bodies called from the template — only `$store` references that appear directly in the template or in `$:` reactive statements are wired to re-render. So the action column never updated on locale switch even though every other `$t(...)` in the same component did. The fix surfaces `$t` at the call site so Svelte wires the cell to the locale store. When adding any new function that translates inside the History table (or any `#each` body), pass `$t` as an argument from the template; do not rely on the function closing over `$t`. The sentinel placeholder (hover-title showing the raw English `entry.command`) is by design and NOT a bug — it gives the user the original CLI audit string for debugging and should not be i18n'd.
- **v0.7.7 Launch badge i18n (hard boundary)**: the amber `launch` badge next to a launch-type profile candidate in the inherits list (`ProfilePage.svelte`) MUST render `{$t('profiles.typeLaunch')}`, not the hardcoded string `launch`. The key `profiles.typeLaunch` exists in all 10 locales (en=Launch, zh=启动, ja=起動, ko=런치, de=Starten, fr=Lancer, es=Lanzar, pt=Iniciar, ru=Запуск, ar=إطلاق). The badge lets the user see at a glance which candidates are Launch-typed (and therefore subject to the Global<-Launch inheritance block), so localizing it is required — a Chinese user seeing the English word `launch` in an otherwise-Chinese UI is the symptom that triggered this fix. When adding a new profile-type badge anywhere, use the existing `profiles.type<Name>` key; never hardcode the type label.

- **v0.7.8 Locale persistence startup read-order (hard boundary, supersedes v0.7.7 locale sub-entry)**: `setupI18n` in `frontend/src/lib/i18n.ts` MUST NOT seed `localeStore` from a synchronous guess (stale WebView2 `localStorage` or `navigator.language`) before the authoritative durable IPC read resolves. The prior bug: on a Chinese Windows host, stale `localStorage` held `zh` from a prior session; `setupI18n` unconditionally queued `localeStore.set(zh)` synchronously, and once the async `getSetting('locale')` ("en") completed it was too late because the sync-set already overwrote the durable IPC file via a stray `setSetting('locale', resolved)` in the startup path. Fix: `setupI18n` inits the store with the default locale (`en`) for first paint and persists the localStorage hint only (for instant first-render feedback); it MUST NOT call `localeStore.set` with a guessed value and MUST NOT call `setSetting` in the startup path. A new exported `applyPersistedLocale()` awaits the durable `getSetting('locale')` IPC read, calls `localeStore.set(persisted)` authoritatively, updates `localStorage` to match, and seeds the IPC file from `localStorage` ONLY on first run (durable empty + localStorage non-empty). `App.svelte` onMount MUST `import { applyPersistedLocale } from './lib/i18n'` and `await applyPersistedLocale()` AFTER the `fontScale`/`darkMode` read block and BEFORE the first variable-list fetch. The durable IPC file (`%LOCALAPPDATA%\EnvManager\gui-settings.json` via `read_gui_setting`/`write_gui_setting`) is the SINGLE SOURCE OF TRUTH for locale; stale `localStorage` is read-only for first-render hint and never propagated to the durable store at startup. Never re-introduce `getLocaleFromNavigator()` in the first-resolve path (it returns `zh-CN` on Chinese Windows and overwrites an explicit `en`). Never re-introduce an unconditional `setSetting('locale', ...)` in the startup path. When adding a new persisted GUI setting: add a read in the SAME `App.svelte` onMount block, never leave a setting that is written but not read back.
- **v0.7.8 Secret provider activation error i18n keys (hard boundary amendment to v0.7.6 entry)**: the v0.7.6 provider-activation-error i18n ledger is extended with two new keys — `errors.activate.sopsConfig` (matches CLI `sops encryption failed` + `config file not found|no keys provided`, routes to SOPS `.sops.yaml` / `SOPS_PATH` fix guidance) and `errors.activate.opAccounts` (matches CLI `1Password CLI .*No accounts configured|No accounts configured for use with 1Password CLI`, routes to desktop-app-integration / `op account add` / `OP_SERVICE_ACCOUNT_TOKEN` / Connect guidance). Both keys added to ALL 10 translation files. The `localizeError` function in `ProfilePage.svelte` MUST match these patterns before falling through to `errors.activate.generic`. When a new secret-provider activation-failure mode is discovered, add an `errors.activate.<mode>` i18n key to all 10 locales, add a regex branch to `localizeError`, and document the upstream fix in `docs/secret-providers-guide.md`.
- **v0.7.8 Secret provider activation error inline display (hard boundary)**: the GUI provider-change confirmation flow (`requestChangeProvider` -> confirm modal -> `confirmChangeProvider` -> `secretProviderSet`) MUST surface a failed activation probe as an INLINE amber banner directly below the provider `<select>`, NOT only as a toast/modal. `ProfilePage.svelte` keeps a `providerErrorMessage: string | null` reactive state; `confirmChangeProvider`'s catch sets `providerErrorMessage = localizedMessage` (in addition to the legacy `showMessage` toast for redundancy). The inline banner renders `secrets.activeProvider + providerErrorMessage` with a Close button that clears `providerErrorMessage` without side effects. `requestChangeProvider` and `cancelChangeProvider` MUST clear `providerErrorMessage` so a stale error from a prior attempt does not linger. Rationale: a transient toast disappears and the user re-selects blindly, re-hitting the same misconfiguration; the inline banner stays until the user fixes the environment (install module / set env var) and closes it.
- **v0.7.8 Edge-style true-overlay scrollbar (hard boundary)**: `App.svelte` global scrollbar CSS MUST NOT pin `::-webkit-scrollbar { width: ...px }`. Pinning width reserves a dedicated layout track (8px) and contradicts the "float over content, zero layout space" goal that matches Edge/VS Code. The CSS sets `scrollbar-width: thin` + `scrollbar-color` (Firefox fallback, also auto-hide: thumb-color rgba 0 when idle, 0.25 on hover) but leaves `::-webkit-scrollbar` width unset so Chromium renders a transient overlay that does not shift content. `scrollbar-gutter` MUST stay at the browser default (`auto`); never set `scrollbar-gutter: stable` (it reserves space, the opposite of the goal). The thumb `background-color` transition (0.15s) + `border-radius: 8px` is a visual-consistency enhancement over the bare native overlay; the dark-mode block mirrors the light-mode block with white-on-transparent thumb colors. Never re-add `width: 8px` to `::-webkit-scrollbar`.
- **v0.7.9 setupI18n MUST NOT read localStorage on startup (hard boundary)**: `setupI18n` in `frontend/src/lib/i18n.ts` MUST set `const hint = defaultLocale` (always `en`) and MUST NOT use `localStorage.getItem('locale')` to derive the first-paint locale. Empirical evidence (env-manager.log `[gui] setupI18n: hint=en stored="zh"` repeated across three restarts) showed WebView2 localStorage in the portable Tauri build is unreliable across restarts: an explicit `localStorage.setItem('locale','en')` made in a prior session was NOT visible on the next boot — it kept inertly persisting the OLD `zh` that was there before. The prior code derived `hint = normalizeLocale(localStorage.getItem('locale') || defaultLocale)`, which on the bogus-stale-`zh` host made first paint render `zh`, so the UI flashed Chinese before `applyPersistedLocale()` flipped to the durable `en`. localStorage is now a FORWARD-ECHO write target only; it is NEVER read back during startup. The durable IPC store (`gui-settings.json` via `getSetting`/`write_gui_setting`) is the SINGLE authoritative source, read in `applyPersistedLocale()`. First paint is ALWAYS the default locale (`en`), and persisted non-en locales cause exactly ONE authoritative `localeStore.set(authoritative)` after the durable IPC read resolves. This eliminates both the first-paint Chinese flicker AND the intermittent "reverts to zh after restart" symptom. `localStorage.setItem('locale', defaultLocale)` is still called best-effort forward so existing `i18n.test.ts` assertions that localStorage is populated after `setupI18n()` remain true, but it is never consulted as input. Never re-introduce `localStorage.getItem('locale')` in `setupI18n`. When adding a new persisted setting that previously read from localStorage on startup, follow the same rule: read only the durable IPC store, never the framebuffer.
- **v0.7.9 applyPersistedLocale empty-durable MUST stay at default, never seed from localStorage (hard boundary)**: `applyPersistedLocale` in `frontend/src/lib/i18n.ts` MUST NOT seed the durable store from `localStorage` when the durable value is empty. The prior code had an `else if (stored)` branch that seeded `setSetting('locale', normalizeLocale(stored))` from a stale localStorage — on the broken portable build this re-seeded the bogus `zh` over and over, locking every restart back to Chinese. The corrected code has a single terminal `else` branch: empty durable stays at `defaultLocale` (`en`), echoes `localStorage.setItem('locale', defaultLocale)` (still forward-echo only), `localeStore.set(defaultLocale)`, and `setSetting('locale', defaultLocale)` to populate the durable store so subsequent runs leave this branch. This guarantees: (a) first run on a host with no durable value boots en; (b) a host on which `localStorage` was poisoned by a prior buggy build boots en AND repairs itself in one restart. Never re-introduce seeding a persisted GUI setting from localStorage when the durable store is empty.
- **v0.7.9 write_gui_setting atomic + fsync + rename (hard boundary)**: `write_gui_setting` in `frontend/src-tauri/src/main.rs` MUST persist `gui-settings.json` via `write_atomic(&path, &json)` (new sibling helper): serialize the JSON object, create a `.tmp` file in the same directory named `<stem>.<pid>.tmp`, `write_all` the bytes, `sync_all` (fsync) the file, then `std::fs::rename(tmp, path)` to atomically replace the target. The prior direct `std::fs::write(&path, json)` was non-atomic: it truncated the file then chunked the write, so a mid-write kill (tray quit, OS shutdown, WebView2 teardown) could leave the durable file truncated/empty; `read_gui_setting` then parsed `null` and the frontend fell back to the unreliable localStorage (the v0.7.7/v0.7.8 locale-revert root cause). The atomic temp+fsync+rename guarantees the durable file is either fully the previous value or fully the new value, never torn. `write_atomic` MUST `sync_all` BEFORE rename (durability across power loss; without fsync the renamed bytes could live only in the FS page cache and be lost on a crash). On rename failure it MUST remove the temp file. The temp name includes the pid so concurrent writers do not stomp each other (defensive paranoia; the app is still single-writer at the process level). Never replace `write_atomic` with a plain `std::fs::write` for any persisted settings file. When adding a new persisted Rust-side file, route its write through `write_atomic` as well; do not add direct `std::fs::write(&path, ...)` for any user-durable state.
- **v0.7.9 frontend log IPC `frontend_log` (hard boundary)**: `frontend/src-tauri/src/main.rs` MUST register a Tauri command `frontend_log(level: String, message: String) -> ()` that validates level (`error`/`warn`/`debug`/anything else = `info`) and writes a single line `log::error!("[gui] {}", bounded)` / `warn!` / `debug!` / `info!` into the same `tauri-plugin-log` file target as Rust-side logs. `frontend/src/lib/settingsStore.ts` exports `frontendLog(level, message)` which invokes this command with a 2048-char bound and MUST catch and swallow any IPC failure so a logger NEVER throws in the caller. The frontend i18n init (`setupI18n`, `applyPersistedLocale`, `SettingsDialog.switchLocale`, `App.svelte` onMount) MUST emit diagnostic lines via `frontendLog` so the locale race (intermittent zh-revert) is traceable from the single `env-manager.log` file the user already inspects. The log message MUST be a bounded summary string (variable NAME + decision, NEVER a secret value or a raw CLI stdout); the frontend caller is responsible for not stringifying user values. Adding a new startup decision that might regress (any persisted-setting read/write): add a `frontendLog('info'|'warn'|'error', ...)` line at that site.
- **`RunChangeScope` ambiguous-scope rejection**: when a variable exists in both user and system scope, the caller must specify `--scope` explicitly; auto-detection is rejected (no silent pick of user).
- **v0.7.11 PowerShell SecretManagement canonical module name (hard boundary)**: `PowerShellSecretManagementProvider.EnsureVaultRegistered` MUST use `-ModuleName Microsoft.PowerShell.SecretStore` (the canonical PowerShell Gallery package name), NOT the short `-ModuleName Microsoft.SecretStore`. The actual installed module is `Microsoft.PowerShell.SecretStore`; the short form fails with `Could not load ... The specified module 'Microsoft.SecretStore' was not loaded because no valid module file was found in any module directory.` and the activation probe never reaches `Set-Secret`. The `EnsureSecretManagementAvailable` probe already uses `Get-Module -ListAvailable *SecretManagement*` (wildcard) -- a literal `Microsoft.SecretManagement` returns zero even when the canonical `Microsoft.PowerShell.SecretManagement` is correctly installed, so it MUST NOT regress to the literal probe. User-side prerequisite: a fresh `Microsoft.PowerShell.SecretStore` defaults to an interactive password prompt that hangs the headless probe; run `Set-SecretStoreConfiguration -Scope CurrentUser -Authentication None -Password (ConvertTo-SecureString -String 'none' -AsPlainText -Force) -Confirm:$false` once before activating the provider (documented in `docs/secret-providers-guide.md`).
- **v0.7.11 SOPS env var `SOPS_AGE_RECIPIENTS` plural (hard boundary)**: `SopsProvider` MUST forward `SOPS_AGE_RECIPIENTS` (PLURAL, trailing `S`) to the sops subprocess for encryption, not the singular `SOPS_AGE_RECIPIENT`. Per official age docs (https://getsops.io/docs/usage/identities/age/), the decryption env var set is `SOPS_AGE_KEY` / `SOPS_AGE_KEY_FILE` / `SOPS_AGE_KEY_CMD`; `SOPS_AGE_SECRET_KEY` is NOT a real env var and MUST NOT be forwarded. Setting `SOPS_AGE_RECIPIENT` (singular) results in `config file not found and no keys provided through command line options` even when the env var is present.
- **v0.7.11 SOPS file-write no BOM (hard boundary)**: `SopsProvider` MUST write plaintext input JSON with `new UTF8Encoding(false)` (no BOM), NOT `Encoding.UTF8`. `Encoding.UTF8` emits a BOM (`EF BB BF`); sops 3.x rejects BOM-prefixed JSON with `invalid character 'i' looking for beginning of value` when unmarshalling. Both the encrypt-path write (where the CLI writes the plaintext JSON file it hands to sops) and the decrypt-path write (where it writes the sops-decrypted JSON for parsing) MUST use the no-BOM encoding. Never regress these to `Encoding.UTF8`; the rest of the provider may use `Encoding.UTF8` for byte-to-string hashing where BOM presence is irrelevant.
- **v0.7.11 AWS SigV4 request headers (hard boundary)**: `AwsSecretsManagerProvider.CallAwsApi` MUST place `Authorization`, `X-Amz-Target`, `X-Amz-Date`, and `X-Amz-Security-Token` on an `HttpRequestMessage` via `request.Headers.Add`, NOT on the `HttpContent` via `content.Headers.Add`. Adding `Authorization` to `HttpContent` throws `Misused header name, 'Authorization'` because the .NET dispatcher treats it as a content header and validates it against the content-header allow-list. The fix builds an explicit `HttpRequestMessage` and calls `client.SendAsync(request)` instead of `client.PostAsync(uri, content)`. AWS SigV4 `signedHeaders` still list the same header names; only the dispatcher placement changes. The ID/region guards (`AWS_REGION`/`AWS_DEFAULT_REGION` for region, `AWS_ACCESS_KEY_ID`+`AWS_SECRET_ACCESS_KEY` for credentials) are the earlier fail-closed layer; with this fix, a host that passes the env-var guards but supplies bad credentials now fails at the network layer (403/HTTP error), not at the header-dispatch layer.
- **v0.7.11 Rust non-zero exit stderr_hint log (hard boundary)**: `frontend/src-tauri/src/main.rs` `run_cli` MUST emit a `warn!("[run_cli] non-zero exit stderr_hint: {}", scrub_stderr(&stderr))` line whenever `output.status.success()` is `false` AND `stderr.trim()` is non-empty. The `scrub_stderr` helper truncates to 512 chars and masks common secret-bearing patterns (`Bearer `, `token=`, `Token=`, `password=`, `Password=`, `setx `, `OP_SERVICE_ACCOUNT_TOKEN=`, `VAULT_TOKEN=`, `AWS_SECRET_ACCESS_KEY=`, `AWS_SESSION_TOKEN=`) by replacing the 8 chars after the pattern with `<redacted>`. This gives users a visible upstream error shape in `logs/env-manager.log` (provider-activation failures, CLIXML, ...) without pasting secret values. Never extend the bound above 512 chars or remove a secret-pattern entry; this is the durable complement to the inline provider-error banner (v0.7.10).
- **`path dedupe` HashSet isolation**: the dedupe `seen` HashSet only records non-protected entries, so protected entries are never treated as duplicates of themselves or each other.
- **v0.7.12 pwsh async pipe drain (hard boundary)**: `PowerShellSecretManagementProvider.RunPowerShell` MUST drain both stdout and stderr via `BeginOutputReadLine`/`BeginErrorReadLine` event handlers BEFORE `WaitForExit`, then call `WaitForExit` (no timeout) once more after the timed `WaitForExit(30000)` returns. The prior code read stdout/stderr via `ReadToEnd` AFTER `WaitForExit(30000)`; per .NET guidance this deadlocks when either pipe fills before the child exits (`pwsh` blocks writing to a full pipe while we wait for its exit), which was the "pwsh window hang" symptom. The fix lets the child process never block on pipe backpressure, so a large stderr never wedges the 30s window.
- **v0.7.12 locale pre-await + tray i18n race (hard boundary, supersedes v0.7.10's locale-pre-mount local half)**: `frontend/src/lib/i18n.ts` `applyPersistedLocale` MUST `await loadLocaleMessages(authoritative)` BEFORE `localeStore.set(authoritative)` so any reactive listener that reads translated strings synchronously on store change (notably the App.svelte tray i18n updater that calls `get(tStore)('tray.show')` and posts `updateTrayLocale`) sees the translated values rather than fallback English keys. The prior code only `register()`ed the locale's lazy loader; when the tray reactive handler fired on `$locale`, the lazy dynamic `import('./translations/zh.json')` had not resolved yet, so `get(tStore)('tray.show')` returned the fallback English key and the tray showed English Show/Quit even on a Chinese startup. `loadLocaleMessages` reuses the same dynamic import path svelte-i18n uses and then calls `addMessages(loc, mod.default)` directly; failure is soft (frontend logs a warn and lets svelte-i18n's own lazy registry retry). Never skip this pre-load for a non-default locale.
- **v0.7.12 staged PATH move + Apply (hard boundary)**: `PathEditor.svelte` move-up/move-down MUST accumulate adjacent swaps locally on `stagedEntries` (with `stagedActive=true` and entry `.index` preserved as the stable key) and render via `displayEntries = stagedActive ? stagedEntries : entries`. The Apply button runs `applyStagedMoves()`, which re-reads live `listPathEntries(scope)`, and for each target position `i` finds the live registry position of the entry whose original `.index === stagedEntries[i].index`, then calls `movePathEntryUp(realPos)` repeatedly until it reaches position `i`. A mid-stage abort (entry vanished from the registry) throws and does NOT silently reorder other entries; the staged view is preserved so the user can retry or cancel. If an entry's original `.index` is missing from the live list, refuse the whole sequence rather than guessing. This matches the Windows official PowerToys env-var editor flow (no per-click IPC, deferred commit). Never call `movePathEntryUp`/`movePathEntryDown` with `entry.index` from the staged view as if it were the live registry position; during staging they have diverged.
- **v0.7.12 secret reveal / provider-set audit (hard boundary)**: `ProfileRevealSecret` MUST `RecordProfileAudit("profile reveal-secret", profileName, {name: redacted}, {name: redacted, value: revealed})` AFTER successful decrypt and BEFORE writing plaintext to stdout; the audit records the variable NAME + a `revealed` marker, never the plaintext or ciphertext. `RunSecretProviderCommand` set branch MUST `RecordProfileAudit("profile secret-provider set", "(all)", null, {provider: name})` after `SetActiveProvider` returns. `secret-provider list` is a metadata read and MUST NOT be audited (only state-changing atomic transactions are). These two commands close the v0.7.12 history-completeness mandate: now every secret-related atomic transaction (add-secret, edit-secret, remove-secret, reveal-secret, export-secrets, import-secrets, secret-provider rotate, secret-provider set) has an audit record; users see it in the History view via `getOperationLabel(entry.command, $t)` routing to `history.op.profile reveal-secret` / `history.op.profile secret-provider set`.
- **v0.7.12 toggle switch right-bias (GUI-only, not a code invariant)**: the Settings UI toggle width is `w-8` and the OFF/ON thumb translate range is `translate-x-0.5`..`translate-x-3.5` (`h-5 w-8`, thumb `h-3.5 w-3.5`). The narrower track (was `w-9` / `translate-x-0.5`..`translate-x-4`) reduces the right-side empty space in the OFF state so the toggle no longer looks half-on. This is a visual-only change tracked here so a future refactor does not widen it again; the toggle still respects `cliInPath`/`darkMode`/`disabled`/`cliToggleLoading`.
- **v0.7.12 addCliToPath fail-fast 30s (GUI-only)**: `SettingsDialog.toggleCliInPath` MUST wrap `addCliToPath()` and `removeCliFromPath()` in a local `withCliTimeout(p, label)` that races the IPC against a 30s `setTimeout`. If the CLI subprocess wedges, the user sees a typed "CLI failed (timeout after 30s)" toast rather than the toggle staying greyed forever. The promise resolves/rejects clear the timer; viz AGENTS.md fail-loud mandate. Never remove this guard, and never silently swallow the timeout rejection.
- **v0.7.13 history delete read/write classification (hard boundary)**: `is_read_only` in `frontend/src-tauri/src/main.rs` MUST classify `history delete` as WRITE (same as `history undo`), NOT READ. The prior code `!matches!(args.first().map(|s| s.as_str()), Some("undo"))` treated every non-undo history subcommand as read-only, so `history delete` (which mutates `audit.json` by removing an entry) acquired only a read lock, allowing a concurrent write to run simultaneously against the same audit file. The fix adds `Some("delete")` to the write-only match arm: `Some("undo") | Some("delete")`. `history list` remains READ (read-only). Any future history subcommand that mutates `audit.json` MUST be added to this match arm before the command is shipped. This is the same root-cause pattern as the cross-process mutex: one guard in the shared classification function is smaller and safer than per-caller serialization.
- **Backup file validation**: `.json` extension required; writes to `\Windows`, `\Program Files`, `\Program Files (x86)` blocked; 50 MB cap.
- **Disabled-variable restoration**: `toggle` writes a same-scope backup with the original raw registry value and `RegistryValueKind`. Re-enable verifies both exactly before deleting the backup; if both original and backup exist, it refuses the conflict. `list` and `get` project a backup-only disabled variable through the same contract for both user and system scope. Names ending in `_EnvManager_disabled` are internal and cannot be created, addressed, renamed, deleted, or moved directly.
- **v0.7.1 Profile variable/PATH scope selector**: `profile add-var <name> <var> <value> [--scope user|system]` and `profile add-path <name> <dir> [--scope user|system]` accept an optional scope. `ProfileVariable.Scope` (default `"user"`) is persisted in profiles.json and consumed by `ProfileApply` to route the variable to HKCU or HKLM. `ProfileData.PathScopes` is a List<string> parallel to `PathEntries` by index; older profiles.json files written before this field existed load as an empty list and `ProfileApply` treats a missing/out-of-range entry as `"user"` so existing behaviour is unchanged. `ProfileAddVarWithScope`/`ProfileAddPathWithScope` are the argv-parsing wrappers in src/ProfileCommand.cs that resolve `--scope` and delegate to `ProfileAddVar`/`ProfileAddPath`. The GUI add-var/add-path panels expose this as a `<select>` bound to `newVarScope`/`newPathScope`; the API layer (`addProfileVar`/`addProfilePath` in api.ts) forwards it as the trailing `--scope` CLI arg. Never write a profile variable or PATH entry without recording its scope — a missing scope silently defaults to `"user"`, which on a real apply would put a system-intended variable into HKCU.
- **v0.9.14 PATH scope-aware apply/unapply (hard boundary fix)**: `ApplyProfile` and `UnapplyProfile` in `src/ProfileEffective.cs` MUST use `variable.Scope` (not hardcoded `"user"`) for all backup read/write and variable write/delete operations. `GetEffectiveProfileVariables` MUST split PATH entries by `PathScopes` into per-scope synthetic PATH variables (user + system), each carrying the correct `Scope` field — a single hardcoded `"user"` PATH variable silently routes system-scope PATH entries to HKCU. `ResolveProfilePathsWithScopes`/`ResolvePathsWithScopes` in `src/ProfileCommand.cs` preserves `PathScopes` index alignment through inheritance chain resolution (the old `ResolveProfilePaths` returned a flat `List<string>` that lost all scope information). This was the root cause of the "PATH entries not applied when profile enabled" bug: the v0.7.1 scope selector stored `PathScopes` correctly, but `ApplyProfile` never consumed it — all variables and the synthetic PATH were hardcoded to `"user"` scope. Never regress `ApplyProfile`/`UnapplyProfile` to hardcoded `"user"`; always read `variable.Scope`.
- **Profile creation**: profile names are globally unique because commands are name-addressed. `profile create <name> --type launch --target <exe> [--args <args>] [--cwd <dir>]` validates and persists Launch profiles in one write transaction. Never create a Global profile and convert it in a later operation.
- **Frontend cache consistency**: variable and PATH reads use bounded 5-second caches with generation-aware single-flight reads. A successful write advances the generation; older reads must not refill caches or replace newer visible state. The frontend write busy store is reference-counted until the serialized queue drains.
- **PathList non-blocking Directory.Exists (hard boundary)**: `PathList` must NEVER call `Directory.Exists` synchronously on each PATH entry. Slow UNC/network/non-existent paths can block for several seconds each, which caused the real-world 8s protection-page hang. Use `FastDirectoryExists` which wraps `Directory.Exists` in `Task.Run` with a 200ms per-entry timeout; entries that miss the window resolve to `exists=false` (treated as dead) rather than blocking the whole response. This mirrors the resilience pattern PowerToys applies to PATH health checks.
- **Protection page per-call timeout (hard boundary)**: `ProtectionPage.svelte` refresh must NOT use `Promise.all` for `listVariablesRaw()` + `listPathEntries('user')` + `listPathEntries('system')` without per-call timeouts. A single blocked call (e.g. HKLM access denied, or a slow system PATH entry) hung the whole page. The refresh wraps each call in a `withTimeout(p, ms)` helper that collapses failures/timeouts to `null`, then assembles `allPathEntries` from whichever results arrived. The CLI-side `FastDirectoryExists` change above is the primary fix; the frontend timeout is defense in depth so future slow calls cannot regress this.
- **Launch profile apply hard boundary (v0.7.1)**: `ProfileApply` MUST reject any profile whose `ProfileType == "launch"` at entry point with a clear error directing the user to `profile launch <name>`. Launch profiles are *local* only: their variables are injected into a child process env block via `env_clear + env(k,v)` and must NEVER be written to the user registry, never broadcast `WM_SETTINGCHANGE`, never be persisted beyond the launcher process lifetime. Allowing apply would silently demote a Launch (local) profile into a Global-style persistent registry write, violating the locality contract users rely on. The GUI toggle on a Launch profile is disabled with an i18n tooltip (`profiles.launchApplyDisabled`) for the same reason; `profile set-launch` is the only path that converts between global<->launch and it carries full audit + validation.
- **Trailing-backslash + quote argv recovery**: .NET argv tokenization can fold a quoted value ending with `\` together with following args. `LenientArgs.WasArgsCorruptedByTrailingBackslashQuote` detects the signature (an arg element containing both a quote and an embedded long-option/separator marker such as ` --scope `, ` --overwrite`, ` --index `, or standalone ` -- `); only then `LenientArgs.Tokenize` re-scans `Environment.CommandLine` with quotes as terminators and backslashes literal. Recovery replaces known-corrupted argv even when its token count changes. A standalone `--` stays intact for `profile launch <name> -- <extra-args>`. Clean argv from the Tauri/Rust `Command::arg()` path is never re-tokenized. See `src/ArgTokenizer.cs` (recovery dispatch in `src/Program.cs`, launch separator handling in `src/ProfileCommand.cs`).
- **No TxR/TxF (ADR 0014)**: registry mutations MUST NOT use the Windows Kernel Transaction Manager (Transacted Registry TxR / Transactional NTFS TxF) or any dependency on it. Microsoft documents TxF as deprecated and possibly removed in a future Windows release, and its official alternatives list contains no registry multi-value transaction primitive (learn.microsoft.com/en-us/windows/win32/fileio/deprecation-of-txf). The only sanctioned mutation route is compensatory writes + three-layer locking + audit recovery (see the Verified registry writes boundary above and ADR 0014 in docs/adr/0014-no-txr-txf-compensatory-writes.md).
- **Mutation/model test first targets (ADR 0014)**: rename write-verify-delete ordering and apply/unapply backup preservation are the first targets for the Phase 3 differential/model/mutation test upgrade (architecture-recovery spec Phase 3); suites pin them by reference (WritePathSeamTests, ProfileSeamValidationTests) — never re-implement the contract inline in a new suite. Never delete-then-set for renames.
- **Verified registry writes**: `SetVariable` captures the prior raw value and registry kind, writes, then verifies the exact persisted raw value and kind before reporting success. If verification or writing fails, it restores the prior value (or deletes a newly-created value) and verifies that restoration. PATH mutations must use `SetPathEntries` and therefore this transaction; never add a direct PATH registry write path.
- **Rust IPC input validation**: command whitelist, max 64 args, max 32,767 chars per arg, null bytes and control characters rejected.
- **Audit history**: `%LOCALAPPDATA%\EnvManager\audit.json`, capped at 2,000 entries. Undo refuses stale changes unless `--force` explicit. Profile entries carry `Scope = "profile"` and route to `TryUndoProfileAudit`. Phase E (v1.0.0) introduces `%ProgramData%\EnvManager\audit-ledger.jsonl` (append-only hash-chained ledger) as a separate service-level audit; `audit.json` remains the CLI-level audit until Phase E migration. See v1.0.0 Phase E hard boundary.
- **Live test harness (hard boundary after incident)**: any local CLI smoke test against the REAL registry MUST use `scripts/test-with-restore.ps1`. Before testing it snapshots every value name, unexpanded value, and `RegistryValueKind` in `HKCU\Environment` plus accessible `HKLM\SYSTEM\CurrentControlSet\Control\Session Manager\Environment`, and byte-snapshots test-owned Env Manager internal configuration (`profiles.json`, `secretMount.json`, `audit.json`, and protection JSON files). It compares exact before/after state, restores internal configuration after the suite, and on any failure or drift reconciles every registry value (including deleting newly introduced values) before broadcasting `WM_SETTINGCHANGE`. HKLM is skipped, with an explicit warning, only when an administrator-accessible snapshot cannot be created. The harness retains `.reg` and `.json` artifacts on failure or `-KeepBackup`, otherwise removes them on a clean run. Never run raw registry-mutating CLI smoke commands against the host. Backups live in `.test-backups/` (gitignored).
- **Per-session host snapshot (hard boundary after incident)**: before any local dev session that touches the CLI or build, run `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/snapshot-host-env.ps1` once. It exports `HKCU\Environment` and `HKLM\...\Environment` to `.env_bak/<UTC-timestamp>/` and copies Env Manager's internal configs from `%LOCALAPPDATA%\EnvManager\` (profiles.json, secretMount.json, audit.json, protected-vars.json, protected-paths.json, builtin-protected-vars.json, builtin-protected-paths.json) into `.env_bak/<UTC-timestamp>/internal-configs/`. `.env_bak/` is gitignored and NOT auto-cleaned. This is the per-session forensic complement to `test-with-restore.ps1` (which does per-run backup+restore). The prior incident (test harness only backed up HKCU, so a RunSet regression clobbered the user system PATH) motivated both guards.

- **v0.7.1 Incident: CommonProgramW6432 disabled via toggle (2026-07-22)**: `CommonProgramW6432` was a built-in Windows REG_EXPAND_SZ system variable (value `C:\Program Files\Common Files`) but was missing from `protection.defaults.json`, so the `toggle` command could rename it to `CommonProgramW6432_EnvManager_disabled` and delete the original. The user noticed `opencode` and `npm` broke because system PATH resolution depends on this family of variables. Root cause: `protection.defaults.json` listed `CommonProgramFiles` and `CommonProgramFiles(x86)` but omitted `CommonProgramW6432` and `ProgramW6432`. Recovery: wrote `CommonProgramW6432` back to HKLM with the exact original value and REG_EXPAND_SZ kind, verified, deleted the `_EnvManager_disabled` backup, broadcast WM_SETTINGCHANGE. Post-incident fix: added `COMMONPROGRAMW6432` and `PROGRAMW6432` to `protection.defaults.json` and re-seeded `%LOCALAPPDATA%\EnvManager\builtin-protected-vars.json`. All future built-in Windows variables that the OS derives from `ProgramFilesDir`/`ProgramFilesDir(x86)`/`CommonProgramFilesDir`/`CommonW6432` must be added to the protection list before any toggle/delete/set/rename/change-scope path can touch them.
- **v0.7.14 Cross-platform build orchestrator (hard boundary)**: `scripts/build.mjs` is the canonical build orchestrator. It is a Node.js ESM script that runs on Windows, Linux, and macOS with no hardcoded paths (auto-discovers project root via `resolve(__dirname, '..')`). It supports `--arch x64|x86|arm64` for multi-architecture builds, mapping to .NET RIDs (`win-x64`/`win-x86`/`win-arm64`) and Rust triples (`x86_64-pc-windows-msvc`/`i686-pc-windows-msvc`/`aarch64-pc-windows-msvc`). Output: `release/portable/` (dir + ZIP), `release/cli-only/` (dir + ZIP), `release/msi/` (Windows only). The `frontend/scripts/build-all.ps1` is a backward-compatible wrapper that delegates to `build.mjs`. The `frontend/scripts/prebuild.mjs` accepts `--arch` to pass `-r <rid>` to dotnet build. The `frontend/scripts/tauri-build.mjs` accepts `--arch` to override the Rust target triple. The `frontend/scripts/installer.wxs` uses `$(var.Win64)` conditional (`yes` for x64/arm64, `no` for x86) passed via `-dWin64=<val>` to WiX candle. The CI release workflow (`.github/workflows/release.yml`) is `workflow_dispatch` only (manual trigger, no auto-trigger on tags/pushes) and builds x64/x86/arm64 in parallel. Never re-introduce hardcoded paths in build scripts. Never re-add `Win64="yes"` as a literal in installer.wxs. Architecture naming: `x64` (not amd64), `x86` (not x32).
- **Logs never record environment values** - CLI/Rust log command names and argument counts only. Values may contain credentials.
- **v0.9.24 Tab indicator CSS-only (hard boundary)**: App.svelte tab bar indicator MUST be CSS-only (`border-b-2 border-primary` on active tab, `border-transparent` on inactive). The prior JS-based `offsetWidth`/`offsetLeft` absolute-positioned indicator is REMOVED — `updateIndicator`, `indicatorStyle`, `indicatorTransition` variables are deleted. This eliminates the MUI #7187 class bug where `offsetWidth` includes button padding (px-3 = 12px each side), causing the indicator to render 24px wider than the text. Regression test `theme-style-regression.test.ts` now asserts: no `offsetWidth` measurement function exists, no `indicatorStyle`/`indicatorTransition` variables exist, `border-primary` + `border-transparent` classes are present, no absolute indicator div with inline style binding. Never re-introduce JS-based tab indicator width measurement — the CSS-only approach cannot regress because the indicator width is always exactly the button width by construction.
- **v0.9.24 withoutTransition synchronous style removal (hard boundary)**: `applyDarkMode` in App.svelte MUST remove the transition-disabling `<style>` element SYNCHRONOUSLY after `getComputedStyle` forces reflow, NOT in `requestAnimationFrame`. The reemus.dev "ultimate solution" pattern is: `disable → action → getComputedStyle(style).opacity → enable` all in one synchronous frame. The prior code removed the style in `requestAnimationFrame` (async), leaving a 1-frame gap where transitions re-enabled and caused white flash on locked/muted elements. Never re-async the style removal — the synchronous removal is what prevents the flash.
- **v0.9.24 color-scheme on :root (hard boundary)**: `app.css` MUST include `color-scheme: light dark;` on the `:root` selector. Without this, WebView2 (Chromium-based) native scrollbar and form controls stay light-themed even when `data-theme="dark"` is set on `<html>`, causing the white vertical strip on the right side. This is the Microsoft-documented fix (WebView2Feedback #4426, #3696): the `color-scheme` CSS property tells the browser to theme native UI elements to match the page's color scheme. Never remove `color-scheme: light dark` from `:root`; it is the only mechanism that makes WebView2 native scrollbar follow dark mode.
- **v0.9.24 Dark mode destructive contrast (hard boundary, value superseded by v0.9.25)**: All dark theme blocks in `app.css` MUST use a `--destructive` value at or above WCAG 2.1 AA (4.5:1) on dark backgrounds. The prior value `0 72.2% 50.6%` had a contrast ratio of ~3.2:1, below WCAG 2.1 AA minimum of 4.5:1 for normal text. **v0.9.25 correction**: the original v0.9.24 note mislabeled red-400 as #EF4444 — that hex is actually red-500; red-400 is #F87171 per Tailwind's official color palette (https://tailwindcss.com/docs/colors). shadcn v4's dark `--destructive` equals Tailwind red-400 (oklch(0.704 0.191 22.216) ≈ HSL `0 91% 71%`). Never regress dark `--destructive` below WCAG AA 4.5:1.

- **v0.9.25 notesStore reactive store (hard boundary)**: `frontend/src/lib/notesStore.ts` MUST export `notesStore` as a Svelte 4 `Readable<Record<string, VarNote>>`. Components read notes via `$notesStore` auto-subscription (Svelte 4 svelte/store: writable .set/.update synchronously notifies all subscribers across async closures — the only mechanism that guarantees immediate UI re-render after a debounced IPC write; function returns are NOT tracked as reactive deps per Svelte 4 docs). `upsertNote` MUST update the store optimistically (before the 200ms debounced `write_var_notes` IPC), so the UI reflects a new note immediately without requiring a manual refresh. `getNoteSync` is kept as a synchronous snapshot accessor for non-template call sites only — templates MUST use `$notesStore[key]`. Never regress templates to `getNoteSync(key)` reads driven by a `notesTick` writable bump: that pattern does not track the function return as a reactive dependency and causes the "note saved but UI does not update until manual refresh" regression.
- **v0.9.25 Notes tooltip WAI-ARIA APG (hard boundary)**: The hover tooltip for the note button in `Variables.svelte` and `PathEditor.svelte` MUST use `role="tooltip"` on the tooltip element and `aria-describedby` on the trigger button referencing a unique id (W3C WAI-ARIA APG: https://www.w3.org/WAI/ARIA/apg/patterns/tooltip/). The native `title` attribute on the trigger is kept only as a progressive-enhancement fallback for the edit/add label; the actual note content tooltip MUST be the `role="tooltip"` element driven by `$notesStore[key]` so it reflects optimistic store writes immediately. The trigger MUST support keyboard (focus shows tooltip, Escape hides) — never rely on `title` alone (MDN: title not keyboard/touch activatable, SR pronunciation inconsistent).
- **v0.9.25 PathEditor staged move new-array reference (hard boundary)**: `handleMoveUp`/`handleMoveDown` in `PathEditor.svelte` MUST reassign `stagedEntries = [...arr]` (a new array reference) after the swap, NOT `stagedEntries = arr` (same reference). Svelte 4 keyed `{#each ... (entry.index)}` does not reliably re-order DOM when the array reference identity does not change; the new reference is what triggers the keyed diff to move the DOM nodes. Prior `stagedEntries = arr` + `stagedEntries = stagedEntries` reactivity poke left stale DOM order and caused the "first item move down appears to no-op" symptom. Never regress to same-reference reassignment for staged path moves.
- **v0.9.25 PathEditor move focus follows moved row (hard boundary)**: After `handleMoveUp`/`handleMoveDown` swaps, the moved entry's up/down button MUST receive focus (W3C APG listbox-rearrangeable: "focus stays on the moved option so keyboard users can perform consecutive moves without re-finding the row"). The move buttons MUST carry `data-move-up-key={entry.index}` / `data-move-down-key={entry.index}` attributes so the handler can `document.querySelector('[data-move-up-key="' + movedKey + '"]')` and call `.focus()` in a `queueMicrotask`. The moved row also gets a `.just-moved` CSS class bound via `lastMovedKey === entry.index` that animates background-color from primary translucent to transparent over 800ms (transient highlight). Never remove the focus-follow or the highlight — both are required by W3C APG listbox-rearrangeable for accessible reorder UX.
- **v0.9.25 PathEditor dead-entries button reactive disabled (hard boundary)**: The "Remove dead entries" button disabled condition MUST derive from `displayEntries` reactively: `$: liveDeadCount = displayEntries.filter((e) => !e.exists && !e.isProtected).length` and `disabled={actionLoading || liveDeadCount === 0}`. The prior condition `!healthSummary || healthSummary.dead === 0` depended on `healthSummary`, which is only filled by the manual "health check" click and is reset to null on every `refresh()` — so the button stayed grey even when dead entries existed in `displayEntries`, violating MDN aria-disabled + WCAG 2.1 SC 4.1.2 (state must be programmatically determinable from real data) and the shadcn/VS Code baseline (disabled reflects real data, not stale scan artifact). The button label MUST show `({liveDeadCount})` from the reactive value. Never bind the disabled state to `healthSummary` — that field is a manual-scan artifact and is decoupled from live `displayEntries`.
- **v0.9.25 destructive red-400 contrast (supersedes v0.9.24 destructive contrast value)**: All dark theme blocks in `app.css` MUST use `--destructive: 0 91% 71%` (Tailwind red-400 = #F87171 / HSL `0 91% 71%`, ≈7.2:1 contrast on zinc-950 — near AAA, aligned with shadcn v4 official dark `--destructive` = oklch(0.704 0.191 22.216)). The v0.9.24 value `0 84% 67%` (≈6.4:1, passes AA only) is superseded — the new value gives the "Remove dead entries" adjustable red text readability padding to AAA. This follows shadcn-ui issue #281 + PR #3775 (https://github.com/shadcn-ui/ui/pull/3775) and Tailwind's official color palette (https://tailwindcss.com/docs/colors). Never regress dark `--destructive` to `0 84% 67%` (v0.9.24) or `0 72.2% 50.6%` (v0.9.23) or any value below WCAG AA 4.5:1.
- **v0.9.24 Titlebar position:fixed (hard boundary)** The `.titlebar` CSS in `app.css` MUST use `position: fixed; top: 0; left: 0; right: 0;` (Tauri official recommendation from v2.tauri.app/learn/window-customization). The content container in App.svelte MUST add `padding-top: 32px` (via inline style) to account for the fixed titlebar height. The prior `position: relative` caused the titlebar to scroll away with content when the content overflowed. Never regress `.titlebar` to `position: relative` — the fixed positioning ensures the titlebar stays pinned at the top while only the content area scrolls. This is the same pattern Tauri v1/v2 official docs demonstrate.
- **v0.7.15 Build-time CLI version verification (hard boundary)**: `scripts/build.mjs` MUST verify the deployed `env-manager-cli.exe` version matches the `<Version>` in `env-manager.csproj` after the dotnet build step and before assembling the portable/cli-only/msi packages. The probe spawns the CLI with no args (which prints `Env Manager v<X.Y.Z>`) and regex-matches the version string. A mismatch (e.g. stale `bin/Release/net10.0` dir from a prior `TargetFramework` containing a v0.3.0 binary) MUST throw and abort the build. `findCliOutput()` MUST prefer the `net10.0-windows` directory (matching `<TargetFramework>net10.0-windows</TargetFramework>`) before falling back to any other `bin/Release/*` dir. The incident that motivated this guard: the build shipped a v0.3.0 CLI that lacked `path`, `profile`, `history`, and `protection` commands — the GUI showed "Unknown command: history/protection/path/profile" for every page because the deployed binary was a 3-month-old stale artifact. Never remove this guard; it is the only automated check that prevents stale-binary deployment.
- **v0.9.15 Compile-before-package ordering (hard boundary)**: After ANY source file change (`*.cs`, `*.rs`, `*.svelte`, `*.ts`, `*.css`), `scripts/build.mjs` MUST be invoked WITHOUT `--skip-cli`, `--skip-gui`, or `--skip-service` flags. These flags are for incremental packaging ONLY (e.g., rebuilding MSI after a WiX template fix without touching CLI/GUI Rust code). Using skip flags to bypass compilation after a code change produces stale binaries in `release/` and is an AGENTS.md hard boundary violation. The build orchestrator enforces a 5-step compile-then-package sequence: (1) dotnet build CLI + version verification, (2) npm build + tauri build GUI, (2b) cargo build service, (3-4) assemble staging + MSI, (5) ZIP archives. The v0.7.15 CLI version verification guard is the automated backstop that aborts on version mismatch. See `docs/build-and-release.md` "Compile-Before-Package Enforcement" section for the full pipeline documentation.
- **v0.9.20 UI Design System — dual-axis theme tokens (hard boundary)**: The frontend uses a dual-axis theme system: `data-theme="light"|"dark"` (mode axis, on `<html>`) × `data-theme-style="slate"|"blue"|"violet"|"rose"|"cyan"|"amber"` (base color axis, on `<html>`). Token CSS variables are defined in `frontend/src/app.css` as HSL channel values, consumed by Tailwind via `hsl(var(--token))` in `tailwind.config.js`. ALL `dark:` Tailwind prefixes have been removed (0 remaining); dark mode is now driven entirely by `[data-theme="dark"]` attribute selector in `tailwind.config.js` (`darkMode: ["selector", '[data-theme="dark"]']`). ALL inline `<svg>` icons replaced with `lucide-svelte` (Svelte 4 package name, NOT `@lucide/svelte`). Theme persistence uses the existing `gui-settings.json` IPC (`read_gui_setting`/`write_gui_setting`) with keys `themeStyle` and `darkMode` — NEVER `localStorage` (unreliable in portable Tauri build). When migrating a component: (1) replace `dark:bg-gray-*`/`dark:text-gray-*` with token classes (`bg-card`, `bg-muted`, `text-muted-foreground`, `border-border`, etc.) — NO duplicate class names in the same `class=""` attribute; (2) replace inline `<svg>` with `lucide-svelte` import; (3) replace hardcoded `bg-white`/`bg-black`/`text-white`/`bg-blue-600`/`text-gray-400` with tokens (`bg-card`/`bg-background`/`text-primary-foreground`/`bg-primary`/`text-muted-foreground`); (4) semantic colors that are NOT theme-relative (amber warning, green success) MAY stay hardcoded. Unused infrastructure for future shadcn-svelte adoption: `$lib` alias, `components.json`, `bits-ui@0.22.0`, `clsx`, `tailwind-merge`, `cn()` util, `tailwindcss-animate` plugin — these are installed but not yet referenced in source; they are the foundation for Phase 2 shadcn component adoption and MUST NOT be removed.

- **v0.8.0 Phase A SecretMount schema v2 (hard boundary)**: Secret variables in `profiles.json` now reference a `SecretMount` entry in a separate `secretMount.json` file instead of inline secret envelopes. `SecretMount` schema v2 fields: `id`, `provider`, `profileName`, `variableName`, `refreshPolicy` (OnDemand|Periodic), `refreshIntervalSeconds`, `lastFetchedAt`, `lastRotatedAt`, `bootstrapCertThumbprint`. The `secretMount.json` file is stored at `%LOCALAPPDATA%\EnvManager\secretMount.json` (same directory as `profiles.json`). Write ordering is fixed: mount file written first, then profile file. Both use atomic write with fsync (`AtomicWriteProfiles` in C# calls `Flush(true)` before `File.Move`; `AtomicWriteJson` aligned). One-shot migration `MigrateSecretsToMounts` converts inline envelopes to mount references on first load; no dual-write period. Three decrypt paths in `src/ProfileCommand.cs` updated with `ResolveSecretMount`. The C# `AtomicWriteProfiles` and Rust `write_atomic` (in `frontend/src-tauri/src/main.rs`) both fsync before rename — the cross-language fsync alignment is mandatory.
- **v0.9.0 Phase B+C env-manager-service (hard boundary)**: The `service/` Rust crate (`env-manager-service`) is a standalone binary providing secret mount lifecycle management via named pipe IPC. `RuntimeMode` enum (Service/Background/Cli) resolved from `--mode=<x>` argv. Pipe endpoints: Service `\\.\pipe\EnvManager.Service`, Background `\\.\pipe\EnvManager.Background`. `ipc.rs` named pipe server uses `PIPE_FIRST_PIPE_INSTANCE` flag for anti-squatting (prevents pipe hijacking), newline-delimited JSON protocol, 5s connect timeout, per-connection task spawn, 65536-byte request cap. Six IPC methods: ping, status, health, refresh, rotate, reload, shutdown. `reconcile.rs` runs a 300s interval periodic full-scan with 30s first-tick delay (Domain 10: SCM timeout avoidance). It reads `secretMount.json`, filters Periodic mounts, checks `refreshIntervalSeconds` elapsed, updates `lastFetchedAt` atomically. The per-item handler is idempotent — a failed mount does NOT advance metadata and does not affect sibling mounts. `secret_mount_path()` routes to `%ProgramData%\EnvManager\secretMount.json` for Service mode, `%LOCALAPPDATA%\EnvManager\secretMount.json` for Background/CLI mode. The C# `service` CLI subcommand (`RunServiceCommand` in `src/ServiceCommand.cs`) is a thin IPC gateway using `NamedPipeClientStream` with 5s connect timeout. It does NOT perform registry operations directly. In Rust `main.rs`, `"service"` is in `ALLOWED_COMMANDS` only (NOT in `READ_COMMANDS` or `WRITE_COMMANDS`). The tokio runtime build is wrapped in `match` — a runtime creation failure logs an error and exits with code 1 instead of panicking.
- **v0.9.0 Phase B+C cert_bootstrap -EncodedCommand (hard boundary)**: `cert_bootstrap.rs` MUST use `-EncodedCommand <base64(UTF-16LE)>` (via the `run_powershell` helper) instead of `-Command "<escaped script>"`. The prior `-Command` path used `format!` to interpolate `role_id`, `secret_id`, `vault_addr` directly into the PowerShell script string — a command injection risk if an environment variable contains PowerShell metacharacters. The `-EncodedCommand` path (same pattern as v0.7.4 `PowerShellSecretManagementProvider`) eliminates all shell quoting. The Vault AppRole script references `$env:VAULT_ROLE_ID` and `$env:VAULT_SECRET_ID` directly; the Azure SP script references `$env:AZURE_CLIENT_ID` and `$env:AZURE_TENANT_ID` directly. Only `vault_addr`, `cert_thumbprint`, and `tenant_id` (infrastructure metadata, non-secret) are passed via `format!`. Never regress to `-Command` with interpolated secret values.
- **v0.9.5 Phase D cert bootstrap (hard boundary)**: `cert_bootstrap.rs` provides `vault_bootstrap` (AppRole via PowerShell `Invoke-RestMethod`, or TLS client cert via `vault_cert_login` skeleton) and `azure_sp_bootstrap` (SP cert JWT assertion via PowerShell). AWS Roles Anywhere deferred to post-v1.0.0 per ADR 0001 A9. The Vault bootstrap eliminates `VAULT_TOKEN`; the Azure bootstrap eliminates `AZURE_CLIENT_SECRET`. Both return short-lived tokens cached in-memory only. `service_config_path()` resolves to `%ProgramData%\EnvManager\service.config.json` (non-secret infrastructure metadata). Certs stored in `Cert:\LocalMachine\My` with non-exportable private key ACL'd to per-service SID.
- **v0.9.9 Versioned schema migration framework (hard boundary)**: `src/SchemaMigration.cs` implements a registry-based migration system. `ProfileData` now carries a `schemaVersion` field (default 0 for pre-v0.9.9 files). On load, `MigrateProfiles` detects the schema version (by field presence or content inference: inline secret envelopes = v0, mount references = v1, no secrets = current), runs pending steps sequentially (v0->v1: initialize null guards; v1->v2: delegate to existing `MigrateSecretsToMounts`), stamps all profiles with current version, and persists atomically. The migration registry never drops old steps so any version can always migrate forward. Rollback is via the existing .bak file. `SecretMountMigrations` is a parallel registry for `secretMount.json` (currently empty — all mounts created at v2). `DetectProfilesSchemaVersion` uses `IsLikelyDpapiBase64` to infer v0 from inline DPAPI blobs vs mount references.

- **v0.9.20 CheckMenuItem set_enabled after set_text (hard boundary)**: `update_tray_locale_impl` in `frontend/src-tauri/src/main.rs` MUST call `state.lightweight_item.set_enabled(true)` AFTER the `set_text` + `set_checked` sequence. muda on Windows rebuilds the native HMENU item when `set_text` is called, which resets BOTH `checked` AND `enabled` state to defaults (disabled). The previous fix (v0.9.20 commit 2db4fb52) only re-synced `set_checked`, leaving the menu item permanently greyed/disabled. The `update_lightweight_check` function MUST also call `set_enabled(true)` as defense-in-depth on every checkmark sync. This is the same root-cause class as muda issue #11556 (Windows enabled state not auto-updating). Industry standard workaround confirmed via pplx kimi_k26 + exa research: re-assert ALL relevant menu item states (`set_text`, `set_checked`, `set_enabled`) in sequence after any mutation, never assume a single API call preserves cross-state on Windows HMENU.
- **v0.9.9 Full-state export/import (hard boundary)**: `src/StateExportImport.cs` implements CLI `export-state --output <file>` and `import-state --input <file> [--dry-run]`. Export reads all internal config files (profiles.json, secretMount.json, protected-vars/paths, builtin-protected-vars/paths, gui-settings.json, audit.json) from `%LOCALAPPDATA%\EnvManager`, serializes them into a JSON object, and DPAPI-CurrentUser-encrypts the entire blob via `DpapiHelper.EncryptSecret`. The output is a base64 text file. Import decrypts via `DpapiHelper.DecryptSecret`, validates each file as valid JSON, creates .bak backups of existing files, then atomically writes new content (temp + fsync + rename). `--dry-run` validates without writing. Export never includes registry values (those use the existing `backup` command). The archive is DPAPI-bound to the current user + machine. In `main.rs`, `export-state` is classified as READ (concurrent-safe), `import-state` as WRITE (serialized). Both are in `ALLOWED_COMMANDS`. The GUI SettingsDialog has a DR section with Export/Import buttons that call these via IPC, with dry-run validation before the actual import.

- **v0.9.10 Phase E audit ledger unification (hard boundary)**: `src/AuditLedgerMigration.cs` implements CLI-side audit ledger operations. `audit migrate-audit [--dry-run]` migrates `audit.json` entries to the hash-chained `audit-ledger.jsonl` at `%ProgramData%\EnvManager\audit-ledger.jsonl`. Each event carries `prevHash` + `hash = SHA256(prevHash || canonical_json(event_without_hash))`, enabling tamper detection via `audit verify-ledger`. Migration is idempotent (skips if ledger already has events). `audit export-survival-kit [--mount <id>] [--output <file>]` exports a DPAPI-CurrentUser-encrypted survival kit (the kit is machine+user bound). `audit recover-from-ledger` replays create/delete events to reconstruct mount lists after a service crash or config loss. The ledger uses append-only writes with 100MB rotation (atomic rename to .jsonl.1 then .jsonl.2). The CLI still writes to `audit.json` for backward compatibility with the existing GUI history page; the ledger is the authoritative audit trail. The service crate's `audit_ledger.rs` remains the canonical implementation for service-mode ledger writes.

- **v0.9.10 export-survival-kit DPAPI encryption (hard boundary, supersedes v1.0.0 Phase E stub)**: `RunAuditExportSurvivalKit` in `src/AuditLedgerMigration.cs` MUST delegate encryption to `DpapiHelper.EncryptSecret` (DPAPI-CurrentUser via P/Invoke crypt32.dll), NOT the service crate's `export_survival_kit` which calls the CLI as a subprocess. The CLI-side implementation is self-contained: it reads the ledger directly, builds the kit JSON, DPAPI-encrypts it, and writes to a .dpapi file (atomic temp+fsync+rename). Temp plaintext is deleted in a finally block. The service crate's version remains as a fallback when the service is running standalone (it calls the CLI subprocess for encryption). Both paths produce the same .dpapi format. Never extend `export_survival_kit` to use AES-GCM directly in the Rust crate — the AGENTS.md mandate to reuse existing DPAPI infrastructure stands.

- **v0.9.13 Phase 3B Audit AES-GCM at-rest encryption wired (hard boundary)**: `RecordProfileAudit` in `src/ProfileAudit.cs` MUST serialize the audit history to JSON, encrypt via `EncryptAuditContent(auditJson)` (AES-256-GCM with DPAPI-wrapped DEK from `AuditCrypto.cs`), and write the encrypted string via `WriteAtomicUtf8` — NOT `AtomicWriteJson` which writes plaintext. `LoadAuditHistory` in `src/AuditCommand.cs` MUST decrypt the raw audit file content via `DecryptAuditContent(rawContent)` (which auto-detects encrypted vs plain JSON and returns plaintext) before `JsonSerializer.Deserialize`. The `EncryptAuditContent` is fail-open: on crypto error it returns the plaintext so audit recording never breaks. `DecryptAuditContent` returns content unchanged if it does not match the encrypted envelope format, so legacy plaintext audit.json files are backward-compatible until the next write encrypts them. Never write audit content in plaintext via `AtomicWriteJson(AuditFilePath, ...)` again.

- **v0.9.14 InputDialog replaces window.prompt (hard boundary)**: ProfilePage.svelte MUST NOT use `window.prompt()` for profile rename. It uses `openInputDialog(config): Promise<string | null>` from `stores.ts` which renders a Promise-based `InputDialog.svelte` modal (same overlay pattern as ConfirmDialog). The InputDialog supports: initial value, auto-select-all on focus, Enter to confirm, Esc to cancel, empty-value rejection when `allowEmpty=false`. ARIA: `role="dialog"`, `aria-modal="true"`, `aria-labelledby`. The `inputModal` writable store is separate from the `modal` store used by ConfirmDialog to avoid state collision. The old `profiles.renamePrompt` i18n key is replaced by `profiles.renameTitle` + `profiles.renamePlaceholder` in all 10 locale files. Never re-introduce `window.prompt()` in any .svelte file — it shows the ugly WebView2 native popup with title "JavaScript - tauri://localhost/" and is invisible on macOS.

- **v0.9.14 Variable sticky-note annotations (hard boundary)**: User notes for variables/profiles are stored in `%LOCALAPPDATA%\EnvManager\var-notes.json` (schema: `{version:1, notes: Record<string, {key, note, updatedAt, color?, pinned?}>}`). Notes are NEVER written to the registry, profiles.json, or audit.json. The Rust IPC `read_var_notes` / `write_var_notes` commands read/write this file atomically via `write_atomic` (temp + fsync + rename). The frontend `notesStore.ts` wraps IPC with an in-memory cache and 200ms debounced writes. The GUI Variables.svelte renders a sticky-note icon button next to each variable row; clicking opens `openInputDialog` as a note editor. `var-notes.json` is included in `export-state` and `import-state` via `StateExportFiles`. Notes are purely user-internal metadata that do not affect environment variables or the system.

- **v0.9.14 Tab bar Material 3 + WAI-ARIA tablist (hard boundary)**: App.svelte tab bar MUST use `role="tablist"` on the container, `role="tab"` + `aria-selected` + `aria-controls` on each tab button, and `role="tabpanel"` on the content wrapper. Keyboard navigation follows APG pattern: ArrowLeft/Right/Down/Up cycle tabs (loops at ends), Home/End jump to first/last. Roving `tabindex` (`0` for active, `-1` for inactive). Active tab text is font-semibold + accent color; inactive tabs are font-medium + gray. Never remove ARIA roles or keyboard navigation from the tab bar — WCAG 2.1 AA compliance depends on them. **v0.9.24 supersession**: The indicator is now CSS-only (`border-b-2 border-primary` on active tab, `border-transparent` on inactive); the prior JS `offsetWidth`/`offsetLeft` absolute-positioned indicator is REMOVED. This eliminates the MUI #7187 class bug where `offsetWidth` includes button padding (px-3 = 12px each side), making the indicator 24px wider than the text on every regression.

- **v0.9.13 Phase 3C Export-state double-layer encryption wired (hard boundary)**: `RunExportState` in `src/StateExportImport.cs` MUST call `ExportStateEncrypt(jsonState)` (AES-256-GCM payload + DPAPI-wrapped DEK + HMAC-SHA256 integrity, version 2 envelope) — NOT the old `DpapiHelper.EncryptSecret(jsonState)` single-layer path. `RunImportState` MUST call `ExportStateDecrypt(encrypted)` which auto-detects version: v1 (pure DPAPI base64) falls back to `DpapiHelper.DecryptSecret`, v2 (double-layer AES-GCM + DPAPI + HMAC) validates HMAC then decrypts. This ensures backward compatibility with v1 export files while v2 exports get the stronger double-layer protection. The `ExportStateDetectVersion` helper (defined in `AuditCrypto.cs`) peeks at the JSON `version` field to route. Never revert to the single-layer `DpapiHelper.EncryptSecret` path for export-state.

- **v0.9.13 Phase 4F Provider binary hash verification wired (hard boundary)**: `RecordProviderHash` (defined in `src/CliRuntime.cs`, moved from `src/Program.cs` by architecture-recovery issue 21, visibility from `private` to `internal` for cross-class access) MUST be called from `EnsureSopsAvailable` (after the sops `--version` probe succeeds, best-effort via `try { Program.RecordProviderHash("sops", SOPS_BINARY); } catch { }`) and from `EnsureOpAvailable` (after the op `--version` probe succeeds, best-effort via `try { Program.RecordProviderHash("op", OP_BINARY); } catch { }`). The hash (SHA256 of the resolved binary) is stored in `%LOCALAPPDATA%\EnvManager\provider-hash.json` and compared on subsequent runs — a mismatch logs a warning but does NOT block operation (degradation, not fail-closed). The call is best-effort so a hash recording failure cannot prevent secret provider activation. The `SOPS_BINARY` and `OP_BINARY` paths are resolved at class initialization time (`FindSopsBinary`/`FindOpBinary`) and the same resolved path is used for both the version probe and the hash recording so they always hash the exact binary they validated.

- **v0.9.13 Phase 2B/2C/2D/2E/2E+/4A/4B Process security hardening (hard boundary)**: `service/src/process_guard.rs` implements all process-level security hardening. `init()` is called at service startup (service/src/main.rs:100) and MUST apply: (1) `disable_crash_dialogs()` — SetErrorMode(SEM_FAILCRITICALERRORS|SEM_NOGPFAULTERRORBOX|SEM_NOOPENFILEERRORBOX) + WerSetFlags(0x3) via dynamic load of wer.dll; (2) `verify_binary_hash()` — SHA256 of current exe stored at %ProgramData%\EnvManager\service-hash.json, compared on subsequent runs, mismatch logs warn (never fatal); (3) `check_debugger()` — IsDebuggerPresent + CheckRemoteDebuggerPresent via dynamic kernel32 load, also called every reconcile tick (300s) in reconcile.rs; (4) `virtual_lock_secret(buf: &[u8])` — VirtualLock to prevent secret pagefile swap (best-effort, ~30 page limit); (5) `enum_modules_warn_unexpected()` — EnumProcessModules enumeration logs loaded DLLs at trace level; (6) `apply_process_mitigations()` — SetProcessMitigationPolicy via dynamic kernel32 load: ProcessDynamicCodePolicy(ProhibitDynamicCode=1), ProcessExtensionPointDisablePolicy(DisableExtensionPoints=1), ProcessImageLoadPolicy(NoRemoteImages|NoLowMandatoryLabelImages=0x3). Template: Microsoft WSL WslSecurity.cpp ApplyProcessMitigationPolicies(); (7) `check_pagefile_protection()` — reads HKLM\SYSTEM\CurrentControlSet\Control\Session Manager\Memory Management\ClearPageFileAtShutdown via dynamic advapi32 load, warns if 0 or missing (read-only, never auto-modifies system setting); (8) `verify_authenticode()` — deferred stub until code signing is in place (Phase 2 of GitHub release). All checks are best-effort: log warn, never exit (keep service debuggable). 12 unit tests in `process_guard::tests` verify no-panic behavior.

- **v0.9.12 Final alignment release (hard boundary)**: Version bumped to 0.9.12. AGENTS.md version updated to 0.9.12. README.md/README_CN.md version references synced. CLI command reference updated to include `export-state` (read) and `import-state` (write) and audit ledger subcommands (`migrate-audit`, `verify-ledger`, `export-survival-kit`, `recover-from-ledger`). Security audit completed: 0 critical, 0 high (all File.WriteAllText non-atomic writes fixed to temp+move pattern), medium/low findings are expected patterns (process spawn for CLI subprocess, secret variable name references). `AuditLedgerMigration.cs` recovery path fixed to atomic write. `Program.cs` audit encrypt-file path fixed to atomic write.

- **v1.0.0 Phase E audit ledger (hard boundary)**: `audit_ledger.rs` is an append-only hash-chained ledger. `append_event` computes `sha256(prev_hash || current_event_json)`, assigns a GUID `eventId`, writes one JSON line per event. The ledger rotates at 100MB via atomic rename. `verify_ledger` performs full chain verification and detects tampering. `export_survival_kit` exports a JSON archive of per-mount events — currently a plaintext stub; AES-GCM encryption will be added before production use (MUST NOT be used to export real secrets in production). `recover_from_ledger` replays create/delete events to reconstruct the mount list. Ledger path: `%ProgramData%\EnvManager\audit-ledger.jsonl` (Service mode).

- **v0.9.0 Phase B+C service binary build pipeline (hard boundary)**: scripts/build.mjs MUST include a cargo build --release step for the service/ crate after the Tauri GUI build, and copy env-manager-service.exe into both release/portable/ and release/cli-only/. The service binary is a required artifact for the MSI installer (WiX ServiceInstall). findServiceExe() in build.mjs discovers the binary under service/target/release/. Never ship a portable or MSI package without the service binary when targeting x64/arm64.

- **v0.9.0 Phase B+C WiX SCM registration (hard boundary)**: frontend/scripts/installer.wxs MUST include ServiceInstall for EnvManagerService with Start=auto, ErrorControl=normal, Arguments=--mode=service, and ServiceConfig DelayedAutoStart=yes (defers service start to avoid SCM timeout under boot load, Domain 10). The ServiceControl element MUST specify Start=install Stop=both Remove=uninstall Wait=yes. The ProgramDataFolder directory (EnvManagerDataDir) is created with util:PermissionEx ACL for NT SERVICE\EnvManagerService (read+write+execute) and LocalSystem (full) using xmlns:util.

- **v0.9.0 Phase B+C GUI service control panel (hard boundary)**: SettingsDialog.svelte MUST render a service control panel with running/stopped status badge, Ping/Reload/Shutdown buttons, and mount health list. Functions: serviceStatus, serviceHealth, servicePing, serviceReload, serviceShutdown, serviceRefreshMount, serviceRotateMount from api.ts (routed through CLI service subcommand IPC gateway). i18n keys: settings.service.{title,running,stopped,checking,ping,reload,shutdown,healthy,unhealthy,refreshBtn,rotateBtn} in all 10 locales. The panel is non-fatal: if service is not installed, status shows stopped without crashing the settings dialog.
- **v0.9.3 Service persistence across GUI exit (hard boundary)**: GUI exit (tray quit, ExitRequested, OS shutdown, Alt+F4) MUST NOT kill the background service. The tray quit handler and `RunEvent::ExitRequested` callback only clear `SERVICE_PID` (preventing stale references); they MUST NOT call `shutdown_background_service()`. The service is a persistent daemon that survives GUI restarts — its purpose is to keep Periodic mount refresh (Vault/Azure/AWS token rotation) running even when the GUI is closed. PWM research confirmed: Windows child processes do NOT terminate when the parent exits (unless a Job Object with `KILL_ON_JOB_CLOSE` is used); `DETACHED_PROCESS` + `mem::forget` already correctly decouples the process. The ONLY path that kills the service is the `stop_service` Tauri command (user-initiated via the GUI Stop button), which calls `shutdown_background_service()` (IPC shutdown + taskkill fallback). The GUI `handleServiceShutdown` calls `serviceStop()` (direct Tauri IPC, not CLI relay) for faster, more reliable stop. Never re-introduce `shutdown_background_service()` in the tray quit or ExitRequested paths — that was the root cause of the "service dies when GUI exits" bug.

- **v0.9.5 Phase D vault cert login (hard boundary)**: cert_bootstrap.rs vault_cert_login MUST use PowerShell Invoke-RestMethod -Certificate $cert to present the TLS client cert from Cert:\LocalMachine\My by thumbprint. The cert is loaded via Get-ChildItem -Path Cert:\LocalMachine\My | Where-Object { $_.Thumbprint -eq $thumbprint }. vault_addr and thumbprint (infrastructure metadata, non-secret) are passed via format!. No reqwest/rustls dependency — same subprocess pattern as Azure SP cert auth. Never replace with -Command interpolation (regression to injection risk fixed in v0.7.4).

- **v1.0.0 Phase E export_survival_kit DPAPI encryption (hard boundary)**: audit_ledger.rs export_survival_kit MUST delegate encryption to CLI DPAPI-CurrentUser via subprocess (env-manager-cli audit encrypt-file --input <tmp> --output <dpapi>). RunAuditCommand in Program.cs uses SecretProviderManager.Encrypt(plaintext, context). Temp plaintext is deleted after subprocess returns. Output ends in .dpapi (machine+user-bound). Never write the survival kit as plaintext — A10 explicitly requires NOT plaintext.

- **v1.0.0 Phase E audit CLI command (hard boundary)**: src/AuditCommand.cs RunAuditCommand handles audit encrypt-file (write, DPAPI-encrypts a file) and audit list (read, lists audit entries). The audit command is in ValidCommands and Rust ALLOWED_COMMANDS. In Rust is_read_only, audit is composite: audit list = READ, audit encrypt-file = WRITE (same pattern as profile/path/history). The migration script scripts/migrate-audit-to-ledger.ps1 converts audit.json to audit-ledger.jsonl one-shot with hash-chain, renaming audit.json to audit.json.bak afterward.

- **v1.0.0 Phase E audit migration script (hard boundary)**: scripts/migrate-audit-to-ledger.ps1 MUST read %LOCALAPPDATA%\EnvManager\audit.json, convert each entry to a LedgerEvent with GUID eventId and hash-chain (sha256(prevHash + eventJson)), write to audit-ledger.jsonl atomically (write tmp + rename). MUST refuse to re-migrate if audit-ledger.jsonl already exists and is non-empty. After migration, audit.json renamed to audit.json.bak. Script is idempotent: running it twice is safe (second run skips because ledger already exists).

- **v0.9.1 run_cli async + spawn_blocking (hard boundary)**: `run_cli` in `frontend/src-tauri/src/main.rs` MUST be `async fn` and use `tauri::async_runtime::spawn_blocking` to execute `cmd.output()`. The prior synchronous `fn run_cli` + `cmd.output()` blocked the tokio async executor during long-running CLI operations (secret-provider activation probe up to 30s, network calls), causing the GUI to appear frozen. `spawn_blocking` moves the blocking IO to a dedicated thread pool so other async commands (tray, settings, window events) remain responsive. The RwLock is acquired INSIDE the spawn_blocking closure (not outside) so the lock is held only for the duration of the actual subprocess execution. Never revert `run_cli` to a synchronous `fn`; any new Tauri command that wraps a blocking CLI call MUST follow the same async + spawn_blocking pattern.

- **v0.9.1 Service file logging (hard boundary)**: `service/src/main.rs` MUST initialize `env_logger` with a file target (not the default stderr). The log file path is `%ProgramData%\EnvManager\env-manager-service.log` in Service mode and `%LOCALAPPDATA%\EnvManager\env-manager-service.log` in Background/CLI mode. The prior `env_logger::init()` wrote to stderr, which in Session 0 (Windows service session) goes nowhere — service logs were silently lost. The mode is resolved BEFORE logger init (the log path depends on mode). If the log file cannot be opened, fall back to stderr with a warning. Log level is Info by default; `RUST_LOG` env var overrides via `from_default_env()`. Never revert to bare `env_logger::init()`.

- **v0.9.1 GUI provider-change loading state (hard boundary)**: `ProfilePage.svelte` `confirmChangeProvider` MUST set `providerChanging = true` before `await secretProviderSet(target)` and `false` in a `finally` block. The confirm modal button MUST be `disabled={providerChanging}` with `disabled:opacity-50 disabled:cursor-wait` styling and an inline SVG spinner during the await. The prior code had no loading state — the user clicked confirm, the GUI awaited a 30s pwsh probe with no visual feedback, and the UI appeared frozen. The `providerChanging` guard also prevents double-click re-entry. Never remove the `finally { providerChanging = false }` block or the disabled attribute.

### Agent Safety Guidelines

1. Always use `--scope user` for non-interactive workflows. System scope requires elevation and may fail silently.
2. Call `agents --json` first to discover the full command contract, safety boundaries, and async support.
3. Read commands are safe to batch concurrently. Write commands are serialized - do not fire multiple writes in parallel.
4. Never delete critical system variables. Always backup first.
5. Profile names: 1-255 chars, no null bytes, newlines, carriage returns. Variable names in profiles: no `=`.
6. Backup files: `.json` extension, not in system directories, under 50 MB.

### MSI installer (ADR-0009, v0.9.27)

- **No `ServiceControl/@Start="install"`**: silent install must not block on the EnvManager service's first boot (DPAPI + named-pipe init can exceed the 30-s SCM handshake ceiling, producing an apparent hang in `StartServices`). Service is `Start="auto"` (next boot) or started on demand from the GUI.
- **No `ServiceControl/@Wait="yes"` on `Stop`/`Remove`**: all stop/remove actions must be `Wait="no"` so silent uninstall and silent upgrade stay non-blocking.
- **No `sc.exe` deferred CustomActions**: failure/restart policy must be declared via `util:ServiceConfig` inside `ServiceInstall` so it lives inside the MSI transaction.
- **MajorUpgrade MUST pin `Schedule="afterInstallExecute"`** so `RemoveExistingProducts` runs after `InstallExecute`; the old product's files never disappear while its service is still running.
- **Old-service stop on upgrade MUST be a dedicated component** conditioned on `WIX_UPGRADE_DETECTED` (current: `StopOldServiceOnUpgrade`), not the sibling `ServiceControl Stop="both"` in the new-service component.
- Any installer change must pass `frontend/src/installer-wxs-msi-0.9.27.test.ts` (Vitest string-gate) AND the 4-step silent msiexec runbook in `docs/build-and-release.md` before commit.

### MSI hygiene (v0.9.28, extends ADR-0010)

- **`Guid="*"` on non-renameable components only**: the three binaries (`GuiExecutable`, `CliExecutable`, `ServiceExecutable`) pin explicit GUIDs; every other component (libraries, JSON, AGENTS.cli.md, shortcuts, registry keypaths, WebView loader) keeps `Guid="*"`. WiX v3 `Guid="*"` generates a deterministic, path-derived GUID (FireGiant docs: from install dir + KeyPath filename) that is build-stable; pinning only the rename-prone three covers the only failure mode that matters (rename / arch flip across releases).
- **Per-component `RemoveFile Name="*.*" On="uninstall"` on every INSTALLDIR file component**, plus ONE unconditional `RemoveFolder On="uninstall"` on INSTALLDIR itself (currently owned by `EnvManagerDataComponent`, *not* the WIX_UPGRADE_DETECTED-conditional `StopOldServiceOnUpgrade`). **NEVER use `util:RemoveFolderEx`** anywhere in this project: recursive traversal is excessive power for a fixed known INSTALLDIR payload (MSI component ref-counting already cleans installed files; the wildcard `RemoveFile` set is only a residue backstop). The pre-3.14.1 CVE-2024-29188 junction-traversal risk IS patched at our pinned 3.14.1 - decision is power-vs-need, not CVE-driven.
- **`AGENTS.cli.md` stays in INSTALLDIR root** (Decision 3 of grill-plan-msi-residual-guid-v0928). It is a 5 KB payload file, not state; P0 wildcard wipes it with everything else on uninstall.
- Any change to `installer.wxs` repasses **both** string-gate suites (`installer-wxs-msi-0.9.27.test.ts` AND `installer-wxs-msi-0.9.28.test.ts`) AND the 4-step silent msiexec runbook asserting zero-residue under `%ProgramFiles%\Env Manager` after the final uninstall.
