{
  "name": "env-manager-tests",
  "version": "0.3.0",
  "description": "Test configuration for Env Manager GUI",
  "private": true,
  "scripts": {
    "test": "vitest",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest --coverage",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@testing-library/svelte": "^3.2.0",
    "@vitest/ui": "^0.34.0",
    "@playwright/test": "^1.40.0",
    "vitest": "^0.34.0",
    "@vitest/coverage-v8": "^0.34.0"
  }
}
